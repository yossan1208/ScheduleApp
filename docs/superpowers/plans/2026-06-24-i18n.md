# i18n（言語切替）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 全22画面のUIテキストを日本語/英語で切り替え可能にし、言語設定をDBに保存する。

**Architecture:** DB に `language` カラムを追加してユーザーごとの言語設定を保持し、ログイン/プロフィールAPIで返す。フロントエンドは `utils/i18n.ts` でテキストを一元管理し、各ページは `t(key)` と日付ヘルパーを呼び出す。言語切替はSCR-45（個人設定）のラジオボタンで行い、次のページ遷移から反映される。

**Tech Stack:** ASP.NET Core 10 (C#) / xUnit + Moq / Vite + vanilla TypeScript / SQL Server 2022

## Global Constraints

- vanilla TypeScript のみ（フレームワーク不使用）
- `export async function mount(app: HTMLElement): Promise<void>` パターン
- エラーメッセージ（`result.error?.message`）は翻訳しない（API が返す日本語のまま）
- 既存の CSS クラス名は変更しない
- `npm run build` でビルドエラーなしを確認してからコミット
- コミットは英語、`feat:` prefix
- テスト: `dotnet test backend/ScheduleApp.Tests`
- ビルド: `cd frontend && npm run build`

---

### Task 1: DB migration + Backend — language 対応

**Files:**
- Create: `migrations/009_add_language_to_users.sql`
- Modify: `backend/ScheduleApp.Api/Models/Entities/User.cs`
- Modify: `backend/ScheduleApp.Api/Models/Dtos/LoginResponse.cs`
- Modify: `backend/ScheduleApp.Api/Models/Dtos/UserProfileResponse.cs`
- Modify: `backend/ScheduleApp.Api/Models/Dtos/UpdateProfileRequest.cs`
- Modify: `backend/ScheduleApp.Api/Services/AuthService.cs`
- Modify: `backend/ScheduleApp.Api/Services/UserSettingService.cs`
- Modify: `backend/ScheduleApp.Tests/Services/UserSettingServiceTests.cs`

**Interfaces:**
- Produces: `LoginResponse.Language`, `UserProfileResponse.Language`, `UpdateProfileRequest.Language` — Task 3 の API 型定義で使用

- [ ] **Step 1: マイグレーションSQLを作成する**

`migrations/009_add_language_to_users.sql` を作成:

```sql
ALTER TABLE users
  ADD language NVARCHAR(10) NOT NULL DEFAULT 'ja';
```

- [ ] **Step 2: マイグレーションを適用する**

SQL Server に接続して SQL を実行する（開発環境: localhost,1433 / sa / ScheduleApp_2024!）:

```bash
# sqlcmd を使う場合
sqlcmd -S localhost,1433 -U sa -P "ScheduleApp_2024!" -d ScheduleApp \
  -i migrations/009_add_language_to_users.sql
```

または SQL Server Management Studio / Azure Data Studio でファイルを開いて実行する。

- [ ] **Step 3: User エンティティに Language プロパティを追加する**

`backend/ScheduleApp.Api/Models/Entities/User.cs` を以下に書き換える:

```csharp
namespace ScheduleApp.Api.Models.Entities;

public class User
{
    public int    Id              { get; set; }
    public string LoginId         { get; set; } = string.Empty;
    public string Name            { get; set; } = string.Empty;
    public string PasswordHash    { get; set; } = string.Empty;
    public int    PersonalColorId { get; set; }
    public int    ThemeColorId    { get; set; }
    public short  Role            { get; set; }
    public int?   GroupId         { get; set; }
    public bool   IsActive        { get; set; } = true;
    public string Language        { get; set; } = "ja";

    public Color? ThemeColor { get; set; }
}
```

- [ ] **Step 4: LoginResponse に Language を追加する**

`backend/ScheduleApp.Api/Models/Dtos/LoginResponse.cs` を以下に書き換える:

```csharp
namespace ScheduleApp.Api.Models.Dtos;

public class LoginResponse
{
    public int    UserId        { get; set; }
    public string Name          { get; set; } = string.Empty;
    public short  Role          { get; set; }
    public string ThemeColorHex { get; set; } = string.Empty;
    public string Language      { get; set; } = "ja";
}
```

- [ ] **Step 5: UserProfileResponse に Language を追加する**

`backend/ScheduleApp.Api/Models/Dtos/UserProfileResponse.cs` を以下に書き換える:

```csharp
namespace ScheduleApp.Api.Models.Dtos;

public class UserProfileResponse
{
    public int    UserId           { get; set; }
    public string LoginId          { get; set; } = string.Empty;
    public string Name             { get; set; } = string.Empty;
    public short  Role             { get; set; }
    public int    PersonalColorId  { get; set; }
    public string PersonalColorHex { get; set; } = string.Empty;
    public int    ThemeColorId     { get; set; }
    public string ThemeColorHex    { get; set; } = string.Empty;
    public string Language         { get; set; } = "ja";
}
```

- [ ] **Step 6: UpdateProfileRequest に Language を追加する**

`backend/ScheduleApp.Api/Models/Dtos/UpdateProfileRequest.cs` を以下に書き換える:

```csharp
namespace ScheduleApp.Api.Models.Dtos;

public class UpdateProfileRequest
{
    public string Name            { get; set; } = string.Empty;
    public int    PersonalColorId { get; set; }
    public int    ThemeColorId    { get; set; }
    public string Language        { get; set; } = "ja";
}
```

- [ ] **Step 7: AuthService で Language をレスポンスにマッピングする**

`backend/ScheduleApp.Api/Services/AuthService.cs` の LoginResponse 生成部分（`var response = new LoginResponse ...` の箇所）を以下に変更する:

```csharp
var response = new LoginResponse
{
    UserId        = user.Id,
    Name          = user.Name,
    Role          = user.Role,
    ThemeColorHex = user.ThemeColor?.HexCode ?? string.Empty,
    Language      = user.Language,
};
```

- [ ] **Step 8: UserSettingService で Language を Get/Update でマッピングする**

`backend/ScheduleApp.Api/Services/UserSettingService.cs` の `UpdateProfileAsync` に `user.Language = request.Language;` を追加し、`MapToResponse` に `Language` を追加する:

```csharp
public async Task<UserProfileResult> UpdateProfileAsync(int userId, UpdateProfileRequest request)
{
    var user = await repo.GetByIdAsync(userId);
    if (user is null)
        return new UserProfileResult(null, "USER_NOT_FOUND");

    var personalColor = await repo.GetColorByIdAsync(request.PersonalColorId);
    if (personalColor is null)
        return new UserProfileResult(null, "USER_COLOR_NOT_FOUND");

    if (user.GroupId.HasValue && await repo.IsColorUsedByGenreAsync(request.PersonalColorId, user.GroupId.Value))
        return new UserProfileResult(null, "USER_COLOR_CONFLICT");

    var themeColor = await repo.GetColorByIdAsync(request.ThemeColorId);
    if (themeColor is null)
        return new UserProfileResult(null, "USER_COLOR_NOT_FOUND");

    user.Name            = request.Name;
    user.PersonalColorId = request.PersonalColorId;
    user.ThemeColorId    = request.ThemeColorId;
    user.ThemeColor      = themeColor;
    user.Language        = request.Language;

    await repo.UpdateUserAsync(user);

    return new UserProfileResult(MapToResponse(user, personalColor), null);
}

private static UserProfileResponse MapToResponse(User user, Color? personalColor) => new()
{
    UserId           = user.Id,
    LoginId          = user.LoginId,
    Name             = user.Name,
    Role             = user.Role,
    PersonalColorId  = user.PersonalColorId,
    PersonalColorHex = personalColor?.HexCode ?? string.Empty,
    ThemeColorId     = user.ThemeColorId,
    ThemeColorHex    = user.ThemeColor?.HexCode ?? string.Empty,
    Language         = user.Language,
};
```

- [ ] **Step 9: テストファイルに Language フィールドを追加するテストを書く**

`backend/ScheduleApp.Tests/Services/UserSettingServiceTests.cs` に以下のテストを追加する（既存テストの `MakeUser` ヘルパーに `Language` を追加し、新テストを追記する）。

まず `MakeUser` を以下に更新:

```csharp
private static User MakeUser(int personalColorId = 2, int themeColorId = 1, string language = "ja") => new()
{
    Id              = 1,
    LoginId         = "admin",
    Name            = "管理者ユーザー",
    PasswordHash    = BCrypt.Net.BCrypt.HashPassword("pass123"),
    Role            = 0,
    PersonalColorId = personalColorId,
    ThemeColorId    = themeColorId,
    GroupId         = 1,
    IsActive        = true,
    ThemeColor      = MakeColor(themeColorId, "#FF8C00"),
    Language        = language,
};
```

既存テスト `GetProfileAsync_ValidUser_ReturnsProfile` に以下のアサーションを追加:

```csharp
Assert.Equal("ja", result.Data.Language);
```

新しいテストを追加:

```csharp
// Language: GetProfile で language が返る
[Fact]
public async Task GetProfileAsync_ValidUser_ReturnsLanguage()
{
    var user         = MakeUser(language: "en");
    var personalColor = MakeColor(2, "#4169E1");

    _repoMock.Setup(r => r.GetByIdAsync(1)).ReturnsAsync(user);
    _repoMock.Setup(r => r.GetColorByIdAsync(2)).ReturnsAsync(personalColor);

    var result = await _sut.GetProfileAsync(1);

    Assert.Null(result.ErrorCode);
    Assert.Equal("en", result.Data!.Language);
}

// Language: UpdateProfile で language が保存される
[Fact]
public async Task UpdateProfileAsync_WithLanguageEn_SavesLanguage()
{
    var user         = MakeUser(language: "ja");
    var personalColor = MakeColor(2, "#4169E1");
    var themeColor    = MakeColor(1, "#FF8C00");

    _repoMock.Setup(r => r.GetByIdAsync(1)).ReturnsAsync(user);
    _repoMock.Setup(r => r.GetColorByIdAsync(2)).ReturnsAsync(personalColor);
    _repoMock.Setup(r => r.GetColorByIdAsync(1)).ReturnsAsync(themeColor);
    _repoMock.Setup(r => r.IsColorUsedByGenreAsync(It.IsAny<int>(), It.IsAny<int>())).ReturnsAsync(false);
    _repoMock.Setup(r => r.UpdateUserAsync(It.IsAny<User>())).Returns(Task.CompletedTask);

    var request = new UpdateProfileRequest
    {
        Name            = "テストユーザー",
        PersonalColorId = 2,
        ThemeColorId    = 1,
        Language        = "en",
    };

    var result = await _sut.UpdateProfileAsync(1, request);

    Assert.Null(result.ErrorCode);
    Assert.Equal("en", result.Data!.Language);
    _repoMock.Verify(r => r.UpdateUserAsync(It.Is<User>(u => u.Language == "en")), Times.Once);
}
```

- [ ] **Step 10: テストを実行して全件パスを確認する**

```bash
dotnet test backend/ScheduleApp.Tests
```

Expected: 全テスト PASS（新規 2 件 + 既存テストの `Language == "ja"` アサーションが追加されたもの含む）

- [ ] **Step 11: コミットする**

```bash
git add migrations/009_add_language_to_users.sql \
        backend/ScheduleApp.Api/Models/Entities/User.cs \
        backend/ScheduleApp.Api/Models/Dtos/LoginResponse.cs \
        backend/ScheduleApp.Api/Models/Dtos/UserProfileResponse.cs \
        backend/ScheduleApp.Api/Models/Dtos/UpdateProfileRequest.cs \
        backend/ScheduleApp.Api/Services/AuthService.cs \
        backend/ScheduleApp.Api/Services/UserSettingService.cs \
        backend/ScheduleApp.Tests/Services/UserSettingServiceTests.cs
git commit -m "feat: add language field to user profile and login response"
```

---

### Task 2: `utils/i18n.ts` — 言語ユーティリティ新規作成

**Files:**
- Create: `frontend/src/utils/i18n.ts`

**Interfaces:**
- Produces: `t(key)`, `getLang()`, `setLang(lang)`, `months()`, `daysShort()`, `daysLong()`, `fmtYearMonth(y, m)`, `fmtMonthLabel(m)`, `fmtFullDate(date)`, `fmtCountBadge(n)` — Tasks 3〜9 の全ページで使用

- [ ] **Step 1: `frontend/src/utils/i18n.ts` を作成する**

以下のコードで `frontend/src/utils/i18n.ts` を作成する:

```typescript
type Lang = 'ja' | 'en';

let currentLang: Lang = (localStorage.getItem('language') as Lang) ?? 'ja';

export function getLang(): Lang {
  return currentLang;
}

export function setLang(lang: Lang): void {
  currentLang = lang;
  localStorage.setItem('language', lang);
}

// ─── 日付配列 ──────────────────────────────────────────────────────────
const MONTHS_JA       = ['1月','2月','3月','4月','5月','6月','7月','8月','9月','10月','11月','12月'];
const MONTHS_EN       = ['January','February','March','April','May','June','July','August','September','October','November','December'];
const MONTHS_EN_SHORT = ['Jan','Feb','Mar','Apr','May','Jun','Jul','Aug','Sep','Oct','Nov','Dec'];

const DAYS_SHORT_JA = ['日','月','火','水','木','金','土'];
const DAYS_SHORT_EN = ['Su','Mo','Tu','We','Th','Fr','Sa'];
const DAYS_LONG_JA  = ['日曜日','月曜日','火曜日','水曜日','木曜日','金曜日','土曜日'];
const DAYS_LONG_EN  = ['Sunday','Monday','Tuesday','Wednesday','Thursday','Friday','Saturday'];

export function months(): string[] {
  return currentLang === 'ja' ? MONTHS_JA : MONTHS_EN;
}

export function daysShort(): string[] {
  return currentLang === 'ja' ? DAYS_SHORT_JA : DAYS_SHORT_EN;
}

export function daysLong(): string[] {
  return currentLang === 'ja' ? DAYS_LONG_JA : DAYS_LONG_EN;
}

/** year/month は 1-indexed */
export function fmtYearMonth(year: number, month: number): string {
  return currentLang === 'ja'
    ? `${year}年${month}月`
    : `${MONTHS_EN[month - 1]} ${year}`;
}

/** month は 1-indexed */
export function fmtMonthLabel(month: number): string {
  return currentLang === 'ja' ? `${month}月` : MONTHS_EN_SHORT[month - 1];
}

export function fmtFullDate(date: Date): string {
  const y   = date.getFullYear();
  const m   = date.getMonth() + 1;
  const d   = date.getDate();
  const dow = date.getDay();
  return currentLang === 'ja'
    ? `${y}年${m}月${d}日(${DAYS_SHORT_JA[dow]})`
    : `${DAYS_SHORT_EN[dow]}, ${MONTHS_EN_SHORT[m - 1]} ${d}, ${y}`;
}

/** +N件 / +N more */
export function fmtCountBadge(n: number): string {
  return currentLang === 'ja' ? `+${n}件` : `+${n} more`;
}

// ─── 文言辞書 ──────────────────────────────────────────────────────────
const dict: Record<Lang, Record<string, string>> = {
  ja: {
    // Common
    'common.loading':       '読み込み中…',
    'common.error.fetch':   'データの取得に失敗しました',
    'common.error.network': '通信エラーが発生しました',
    'common.error.save':    '保存に失敗しました',
    'common.allDay':        '終日',

    // Login
    'login.id':              'ID',
    'login.password':        'パスワード',
    'login.submit':          'ログイン',
    'login.error.invalid':   'IDまたはパスワードが違います',
    'login.error.disabled':  'アカウントが無効です',
    'login.error.noGroup':   'グループに加入してください',
    'login.error.generic':   'ログインに失敗しました',

    // Home
    'home.error': '予定を取得できませんでした',

    // Day
    'day.empty':         '予定なし',
    'day.deleteBtn':     '削除',
    'day.deleteConfirm': 'この予定を削除しますか？',
    'day.fetchError':    '予定を取得できませんでした',
    'day.sheetError':    '取得に失敗しました',
    'day.self':          '自分',
    'day.other':         '他のメンバー',

    // Schedule new/edit
    'schedule.new.title':          '予定追加',
    'schedule.edit.title':         '予定編集',
    'schedule.label.title':        'タイトル',
    'schedule.label.genre':        'ジャンル',
    'schedule.label.date':         '日付',
    'schedule.label.startTime':    '開始',
    'schedule.label.endTime':      '終了',
    'schedule.label.visibility':   '公開設定',
    'schedule.label.memo':         '詳細メモ',
    'schedule.label.notif':        '通知',
    'schedule.genre.placeholder':  'ジャンルを選択',
    'schedule.visibility.public':  '公開',
    'schedule.visibility.private': '非公開',
    'schedule.allDay':             '終日',
    'schedule.notif.none':         'なし',
    'schedule.notif.atTime':       '予定時刻',
    'schedule.notif.before':       '分前',
    'schedule.save':               '保存する',
    'schedule.delete':             '削除する',
    'schedule.memo.add':           '📝 詳細メモを追加…',
    'schedule.error.noTitle':      'タイトルを入力してください',
    'schedule.error.noGenre':      'ジャンルを選択してください',
    'schedule.error.noGenreList':  'ジャンルがまだ作成されていません',
    'schedule.error.fetchGenre':   'ジャンルを取得できませんでした',
    'schedule.history.empty':      '履歴がありません',
    'schedule.history.loading':    '読み込み中…',
    'schedule.history.error':      '取得に失敗しました',

    // Notes
    'notes.title':    '共有事項',
    'notes.new':      '+ 新規',
    'notes.archive':  'アーカイブ',
    'notes.empty':    'ノートがありません',
    'notes.error':    'ノートを取得できませんでした',

    // Notes form
    'notes.form.createTitle':   'ノートを作成',
    'notes.form.editTitle':     'ノートを編集',
    'notes.form.label':         'ノート名',
    'notes.form.placeholder':   'ノート名を入力',
    'notes.form.submit.create': '作成する',
    'notes.form.submit.edit':   '更新する',
    'notes.form.error.empty':   'ノート名を入力してください',
    'notes.form.error.save':    '保存に失敗しました',

    // Notes archive
    'notes.archive.title':         'アーカイブ',
    'notes.archive.empty':         'アーカイブされたノートはありません',
    'notes.archive.error':         '取得できませんでした',
    'notes.archive.deleteConfirm': 'このノートを完全に削除しますか？この操作は取り消せません。',
    'notes.archive.deleteError':   '削除に失敗しました',

    // Memos
    'memos.archiveConfirm': 'このノートをアーカイブに移動しますか？',
    'memos.archiveError':   'アーカイブに失敗しました',
    'memos.createError':    'メモの作成に失敗しました',
    'memos.empty':          'メモがありません',
    'memos.error':          'メモを取得できませんでした',
    'memos.noTitle':        '（タイトルなし）',

    // Memo edit
    'memo.edit.error':         'メモを取得できませんでした',
    'memo.edit.deleteConfirm': 'このメモを削除しますか？',
    'memo.edit.deleteError':   '削除に失敗しました',
    'memo.edit.saving':        '保存中…',
    'memo.edit.saved':         '保存しました',
    'memo.edit.saveFail':      '保存に失敗',
    'memo.edit.editing':       '編集中…',

    // Settings hub
    'settings.title':              'Settings',
    'settings.menu.profile':       '個人用設定',
    'settings.menu.notifications': '通知設定',
    'settings.menu.genres':        'ジャンル一覧',
    'settings.menu.password':      'パスワード設定',
    'settings.menu.admin':         '管理者用設定',

    // Profile
    'profile.title':               '個人設定',
    'profile.label.name':          '名前',
    'profile.label.personalColor': '個人カラー',
    'profile.label.themeColor':    'テーマカラー',
    'profile.label.language':      '言語',
    'profile.lang.ja':             '日本語',
    'profile.lang.en':             'English',
    'profile.save':                '設定する',
    'profile.saved':               '設定を保存しました',
    'profile.error.noName':        '名前を入力してください',
    'profile.error.noPersonal':    '個人カラーを選択してください',
    'profile.error.noTheme':       'テーマカラーを選択してください',
    'profile.error.colorConflict': '選択した個人カラーは、すでにジャンルで使用されています',
    'profile.error.save':          '更新に失敗しました',
    'profile.placeholder.name':    '名前を入力',

    // Password
    'password.title':                  'パスワード設定',
    'password.placeholder.current':    '古いパスワードを入力',
    'password.placeholder.new':        '新しいパスワードを入力',
    'password.placeholder.confirm':    '確認用',
    'password.save':                   '設定する',
    'password.error.empty':            'すべてのフィールドを入力してください',
    'password.error.mismatch':         '現在のパスワードが正しくありません',
    'password.error.confirmMismatch':  '新しいパスワードと確認用が一致しません',
    'password.error.generic':          'エラーが発生しました',
    'password.saved':                  'パスワードを変更しました',

    // Notifications
    'notif.title':              '通知設定',
    'notif.empty':              'ジャンルがまだ作成されていません',
    'notif.error':              '設定の取得に失敗しました',

    // Genres
    'genres.title':  'ジャンル一覧',
    'genres.add':    '+ 新規追加する',
    'genres.empty':  'ジャンルがまだ作成されていません',
    'genres.error':  'データの取得に失敗しました',

    // Genre form
    'genres.form.createTitle':     'ジャンル作成',
    'genres.form.editTitle':       'ジャンル編集',
    'genres.form.label.name':      'ジャンル名',
    'genres.form.label.color':     'カラー',
    'genres.form.submit.create':   '作成する',
    'genres.form.submit.edit':     '更新する',
    'genres.form.delete':          '削除する',
    'genres.form.error.noName':    'ジャンル名を入力してください',
    'genres.form.error.noColor':   'カラーを選択してください',
    'genres.form.error.conflict':  'このジャンル名はすでに使われています',
    'genres.form.error.save':      '保存に失敗しました',
    'genres.form.error.delete':    '削除に失敗しました',
    'genres.form.deleteConfirm':   'このジャンルを削除しますか？',
    'genres.form.fetchError':      '取得に失敗しました',

    // Admin hub
    'admin.title':          '管理者設定',
    'admin.menu.users':     'ユーザー管理',
    'admin.menu.groups':    'グループ作成',

    // Admin users
    'admin.users.title':          'ユーザー管理',
    'admin.users.new':            '＋ 新規',
    'admin.users.empty':          'ユーザーがいません',
    'admin.users.deactivate':     '無効化',
    'admin.users.deactivated':    '無効化済み',
    'admin.users.error':          '読み込みに失敗しました',
    'admin.users.deactivateError':'無効化に失敗しました',
    'admin.users.role.admin':     '管理者',
    'admin.users.role.gl':        'GL',
    'admin.users.role.general':   '一般',

    // Admin user form
    'admin.user.form.title':                   'アカウント作成',
    'admin.user.form.loginId':                 'Login ID',
    'admin.user.form.name':                    '名前',
    'admin.user.form.password':                '初期パスワード',
    'admin.user.form.role':                    'ロール',
    'admin.user.form.submit':                  '作成する',
    'admin.user.form.error.loginIdConflict':   'このLoginIdはすでに使われています',
    'admin.user.form.error.generic':           '作成に失敗しました',
    'admin.user.form.error.required':          'すべての項目を入力してください',

    // Admin group form
    'admin.group.form.title':            'グループ作成',
    'admin.group.form.name':             'グループ名',
    'admin.group.form.members':          'メンバー選択',
    'admin.group.form.noUsers':          'グループ未所属のユーザーがいません',
    'admin.group.form.submit':           '作成する',
    'admin.group.form.error.noName':     'グループ名を入力してください',
    'admin.group.form.error.noMember':   'メンバーを1人以上選択してください',
    'admin.group.form.error.save':       '作成に失敗しました',
  },

  en: {
    // Common
    'common.loading':       'Loading…',
    'common.error.fetch':   'Failed to load data',
    'common.error.network': 'Network error',
    'common.error.save':    'Failed to save',
    'common.allDay':        'All day',

    // Login
    'login.id':              'ID',
    'login.password':        'Password',
    'login.submit':          'Login',
    'login.error.invalid':   'Invalid ID or password',
    'login.error.disabled':  'Account is disabled',
    'login.error.noGroup':   'Please join a group',
    'login.error.generic':   'Login failed',

    // Home
    'home.error': 'Failed to load schedules',

    // Day
    'day.empty':         'No schedules',
    'day.deleteBtn':     'Delete',
    'day.deleteConfirm': 'Delete this schedule?',
    'day.fetchError':    'Failed to load schedules',
    'day.sheetError':    'Failed to load',
    'day.self':          'Me',
    'day.other':         'Others',

    // Schedule new/edit
    'schedule.new.title':          'Add Schedule',
    'schedule.edit.title':         'Edit Schedule',
    'schedule.label.title':        'Title',
    'schedule.label.genre':        'Genre',
    'schedule.label.date':         'Date',
    'schedule.label.startTime':    'Start',
    'schedule.label.endTime':      'End',
    'schedule.label.visibility':   'Visibility',
    'schedule.label.memo':         'Notes',
    'schedule.label.notif':        'Notification',
    'schedule.genre.placeholder':  'Select Genre',
    'schedule.visibility.public':  'Public',
    'schedule.visibility.private': 'Private',
    'schedule.allDay':             'All day',
    'schedule.notif.none':         'None',
    'schedule.notif.atTime':       'At time',
    'schedule.notif.before':       'min before',
    'schedule.save':               'Save',
    'schedule.delete':             'Delete',
    'schedule.memo.add':           '📝 Add notes…',
    'schedule.error.noTitle':      'Please enter a title',
    'schedule.error.noGenre':      'Please select a genre',
    'schedule.error.noGenreList':  'No genres created yet',
    'schedule.error.fetchGenre':   'Failed to load genres',
    'schedule.history.empty':      'No history',
    'schedule.history.loading':    'Loading…',
    'schedule.history.error':      'Failed to load',

    // Notes
    'notes.title':    'Shared Notes',
    'notes.new':      '+ New',
    'notes.archive':  'Archive',
    'notes.empty':    'No notes',
    'notes.error':    'Failed to load notes',

    // Notes form
    'notes.form.createTitle':   'Create Note',
    'notes.form.editTitle':     'Edit Note',
    'notes.form.label':         'Note name',
    'notes.form.placeholder':   'Enter note name',
    'notes.form.submit.create': 'Create',
    'notes.form.submit.edit':   'Update',
    'notes.form.error.empty':   'Please enter a note name',
    'notes.form.error.save':    'Failed to save',

    // Notes archive
    'notes.archive.title':         'Archive',
    'notes.archive.empty':         'No archived notes',
    'notes.archive.error':         'Failed to load',
    'notes.archive.deleteConfirm': 'Permanently delete this note? This cannot be undone.',
    'notes.archive.deleteError':   'Failed to delete',

    // Memos
    'memos.archiveConfirm': 'Move this note to archive?',
    'memos.archiveError':   'Failed to archive',
    'memos.createError':    'Failed to create memo',
    'memos.empty':          'No memos',
    'memos.error':          'Failed to load memos',
    'memos.noTitle':        '(No title)',

    // Memo edit
    'memo.edit.error':         'Failed to load memo',
    'memo.edit.deleteConfirm': 'Delete this memo?',
    'memo.edit.deleteError':   'Failed to delete',
    'memo.edit.saving':        'Saving…',
    'memo.edit.saved':         'Saved',
    'memo.edit.saveFail':      'Save failed',
    'memo.edit.editing':       'Editing…',

    // Settings hub
    'settings.title':              'Settings',
    'settings.menu.profile':       'Personal Settings',
    'settings.menu.notifications': 'Notifications',
    'settings.menu.genres':        'Genres',
    'settings.menu.password':      'Password',
    'settings.menu.admin':         'Admin Settings',

    // Profile
    'profile.title':               'Personal Settings',
    'profile.label.name':          'Name',
    'profile.label.personalColor': 'Personal Color',
    'profile.label.themeColor':    'Theme Color',
    'profile.label.language':      'Language',
    'profile.lang.ja':             '日本語',
    'profile.lang.en':             'English',
    'profile.save':                'Save',
    'profile.saved':               'Settings saved',
    'profile.error.noName':        'Please enter a name',
    'profile.error.noPersonal':    'Please select a personal color',
    'profile.error.noTheme':       'Please select a theme color',
    'profile.error.colorConflict': 'The selected personal color is already used by a genre',
    'profile.error.save':          'Failed to update',
    'profile.placeholder.name':    'Enter your name',

    // Password
    'password.title':                 'Password',
    'password.placeholder.current':   'Current password',
    'password.placeholder.new':       'New password',
    'password.placeholder.confirm':   'Confirm',
    'password.save':                  'Change',
    'password.error.empty':           'Please fill in all fields',
    'password.error.mismatch':        'Current password is incorrect',
    'password.error.confirmMismatch': 'New passwords do not match',
    'password.error.generic':         'An error occurred',
    'password.saved':                 'Password changed',

    // Notifications
    'notif.title':  'Notifications',
    'notif.empty':  'No genres to configure',
    'notif.error':  'Failed to load settings',

    // Genres
    'genres.title':  'Genres',
    'genres.add':    '+ Add new',
    'genres.empty':  'No genres created yet',
    'genres.error':  'Failed to load data',

    // Genre form
    'genres.form.createTitle':     'Create Genre',
    'genres.form.editTitle':       'Edit Genre',
    'genres.form.label.name':      'Genre name',
    'genres.form.label.color':     'Color',
    'genres.form.submit.create':   'Create',
    'genres.form.submit.edit':     'Update',
    'genres.form.delete':          'Delete',
    'genres.form.error.noName':    'Please enter a genre name',
    'genres.form.error.noColor':   'Please select a color',
    'genres.form.error.conflict':  'This genre name is already in use',
    'genres.form.error.save':      'Failed to save',
    'genres.form.error.delete':    'Failed to delete',
    'genres.form.deleteConfirm':   'Delete this genre?',
    'genres.form.fetchError':      'Failed to load',

    // Admin hub
    'admin.title':          'Admin Settings',
    'admin.menu.users':     'User Management',
    'admin.menu.groups':    'Create Group',

    // Admin users
    'admin.users.title':          'User Management',
    'admin.users.new':            '+ New',
    'admin.users.empty':          'No users',
    'admin.users.deactivate':     'Deactivate',
    'admin.users.deactivated':    'Deactivated',
    'admin.users.error':          'Failed to load',
    'admin.users.deactivateError':'Failed to deactivate',
    'admin.users.role.admin':     'Admin',
    'admin.users.role.gl':        'GL',
    'admin.users.role.general':   'General',

    // Admin user form
    'admin.user.form.title':                   'Create Account',
    'admin.user.form.loginId':                 'Login ID',
    'admin.user.form.name':                    'Name',
    'admin.user.form.password':                'Initial Password',
    'admin.user.form.role':                    'Role',
    'admin.user.form.submit':                  'Create',
    'admin.user.form.error.loginIdConflict':   'This Login ID is already taken',
    'admin.user.form.error.generic':           'Failed to create account',
    'admin.user.form.error.required':          'Please fill in all fields',

    // Admin group form
    'admin.group.form.title':            'Create Group',
    'admin.group.form.name':             'Group name',
    'admin.group.form.members':          'Select members',
    'admin.group.form.noUsers':          'No ungrouped users available',
    'admin.group.form.submit':           'Create',
    'admin.group.form.error.noName':     'Please enter a group name',
    'admin.group.form.error.noMember':   'Please select at least one member',
    'admin.group.form.error.save':       'Failed to create group',
  },
};

export function t(key: string): string {
  return dict[currentLang][key] ?? dict['ja'][key] ?? key;
}
```

- [ ] **Step 2: ビルドして型エラーがないことを確認する**

```bash
cd frontend && npm run build
```

Expected: ビルドエラーなし（i18n.ts は他からまだ import されていないが、単体でビルドされる）

- [ ] **Step 3: コミットする**

```bash
git add frontend/src/utils/i18n.ts
git commit -m "feat: add i18n utility with ja/en dictionary and date helpers"
```

---

### Task 3: API型定義 + ログイン時の language 保存

**Files:**
- Modify: `frontend/src/api/auth.ts`
- Modify: `frontend/src/api/settings.ts`
- Modify: `frontend/src/pages/login/index.ts`

**Interfaces:**
- Consumes: `setLang()` from `utils/i18n.ts` (Task 2)
- Produces: `language` in localStorage — Tasks 4〜9 の全ページで `getLang()` が正しく動作する前提

- [ ] **Step 1: `api/auth.ts` に `language` を追加する**

`frontend/src/api/auth.ts` を以下に書き換える:

```typescript
import { api } from './client';

export interface LoginRequest {
  loginId: string;
  password: string;
}

export interface LoginResponse {
  userId: number;
  name: string;
  role: number;
  themeColorHex: string;
  language: string;
}

export const auth = {
  login: (body: LoginRequest) =>
    api.post<LoginResponse>('/auth/login', body),

  logout: () =>
    api.post<null>('/auth/logout', {}),
};
```

- [ ] **Step 2: `api/settings.ts` に `language` を追加する**

`frontend/src/api/settings.ts` の `UserProfile` と `UpdateProfilePayload` を以下に変更する（他の箇所はそのまま）:

```typescript
// GET /api/users/me, PUT /api/users/me レスポンス
export interface UserProfile {
  userId:           number;
  loginId:          string;
  name:             string;
  role:             number;
  personalColorId:  number;
  personalColorHex: string;
  themeColorId:     number;
  themeColorHex:    string;
  language:         string;
}

// PUT /api/users/me リクエスト
export interface UpdateProfilePayload {
  name:            string;
  personalColorId: number;
  themeColorId:    number;
  language:        string;
}
```

- [ ] **Step 3: ログイン成功時に `language` を localStorage に保存する**

`frontend/src/pages/login/index.ts` のログイン成功時処理（`navigate('/home')` の直前）を以下に変更する:

```typescript
// 変更前
document.documentElement.style.setProperty('--theme-color', result.data.themeColorHex);
localStorage.setItem('currentUserId', String(result.data.userId));
localStorage.setItem('role', String(result.data.role));

// 変更後
document.documentElement.style.setProperty('--theme-color', result.data.themeColorHex);
localStorage.setItem('currentUserId', String(result.data.userId));
localStorage.setItem('role', String(result.data.role));
localStorage.setItem('language', result.data.language);
```

**注意:** ログインページのUIテキスト（「ログイン」ボタン、ラベルなど）は日本語固定のままにする。

- [ ] **Step 4: ビルドして型エラーがないことを確認する**

```bash
cd frontend && npm run build
```

Expected: ビルドエラーなし

- [ ] **Step 5: コミットする**

```bash
git add frontend/src/api/auth.ts \
        frontend/src/api/settings.ts \
        frontend/src/pages/login/index.ts
git commit -m "feat: save language to localStorage on login"
```

---

### Task 4: SCR-45 個人設定 — 言語切替UIと保存

**Files:**
- Modify: `frontend/src/pages/settings/profile/index.ts`
- Modify: `frontend/src/pages/settings/profile/profile.css`

**Interfaces:**
- Consumes: `t()`, `getLang()`, `setLang()` from `utils/i18n.ts` (Task 2)
- Consumes: `language` in `UserProfile` / `UpdateProfilePayload` from `api/settings.ts` (Task 3)

- [ ] **Step 1: profile/index.ts の変更点を確認する**

`frontend/src/pages/settings/profile/index.ts` を読み、現在の構造を把握する（フォームのHTML生成部分、送信処理部分）。

- [ ] **Step 2: profile/index.ts に i18n インポートを追加し、言語UI・ロジックを追加する**

`frontend/src/pages/settings/profile/index.ts` に以下の変更を加える:

**先頭に import を追加:**

```typescript
import { t, getLang, setLang } from '../../../utils/i18n';
```

**フォームHTMLの変更（`profileBody.innerHTML = \`...\`` 部分）:**

`テーマカラー` フィールドの直後、エラー表示 `<p class="profile-form-error"...>` の直前に以下を追加する:

```html
<div class="profile-form-field">
  <label class="profile-form-label">${t('profile.label.language')}</label>
  <div class="profile-lang-radios">
    <label class="profile-lang-radio">
      <input type="radio" name="language" value="ja" ${getLang() === 'ja' ? 'checked' : ''}/>
      ${t('profile.lang.ja')}
    </label>
    <label class="profile-lang-radio">
      <input type="radio" name="language" value="en" ${getLang() === 'en' ? 'checked' : ''}/>
      ${t('profile.lang.en')}
    </label>
  </div>
</div>
```

既存の日本語ハードコード文言を `t()` で置き換える（フォーム内）:
- `'名前'` → `t('profile.label.name')`
- `'個人カラー'` → `t('profile.label.personalColor')`
- `'テーマカラー'` → `t('profile.label.themeColor')`
- `placeholder="名前を入力"` → `placeholder="${t('profile.placeholder.name')}"`
- `'設定する'`（ボタン） → `t('profile.save')`

**フォーム送信処理の変更（`form.addEventListener('submit', ...)` 内）:**

言語の取得を追加:
```typescript
const language = (profileBody.querySelector<HTMLInputElement>('input[name="language"]:checked')?.value ?? 'ja') as 'ja' | 'en';
```

`updateProfile` の呼び出しに `language` を追加:
```typescript
const result = await userSettings.updateProfile({
  name,
  personalColorId: selectedPersonalColorId,
  themeColorId: selectedThemeColorId,
  language,
});
```

成功時に `setLang` を呼ぶ（`navigate('/settings')` の直前）:
```typescript
if (result.success && result.data) {
  document.documentElement.style.setProperty('--theme-color', result.data.themeColorHex);
  setLang(result.data.language as 'ja' | 'en');
  personalCarousel?.destroy();
  themeCarousel?.destroy();
  alert(t('profile.saved'));
  navigate('/settings');
}
```

バリデーションエラーメッセージを `t()` に置き換え:
- `'名前を入力してください'` → `t('profile.error.noName')`
- `'個人カラーを選択してください'` → `t('profile.error.noPersonal')`
- `'テーマカラーを選択してください'` → `t('profile.error.noTheme')`
- `'選択した個人カラーは、すでにジャンルで使用されています'` → `t('profile.error.colorConflict')`
- `'更新に失敗しました'` → `t('profile.error.save')`
- `'プロフィールの取得に失敗しました'` → `t('common.error.fetch')`
- `'カラーデータの取得に失敗しました'` → `t('common.error.fetch')`
- `'データの取得に失敗しました'` → `t('common.error.fetch')`
- `'通信エラーが発生しました'` → `t('common.error.network')`

- [ ] **Step 3: profile.css に言語ラジオのスタイルを追加する**

`frontend/src/pages/settings/profile/profile.css` の末尾に追加:

```css
.profile-lang-radios {
  display: flex;
  gap: 20px;
  margin-top: 4px;
}

.profile-lang-radio {
  display: flex;
  align-items: center;
  gap: 6px;
  font-size: 15px;
  cursor: pointer;
}
```

- [ ] **Step 4: ビルドして確認する**

```bash
cd frontend && npm run build
```

Expected: ビルドエラーなし

- [ ] **Step 5: コミットする**

```bash
git add frontend/src/pages/settings/profile/index.ts \
        frontend/src/pages/settings/profile/profile.css
git commit -m "feat: add language radio to profile settings"
```

---

### Task 5: カレンダーページの i18n 対応（home / day / week / year）

**Files:**
- Modify: `frontend/src/pages/home/index.ts`
- Modify: `frontend/src/pages/day/index.ts`
- Modify: `frontend/src/pages/week/index.ts`
- Modify: `frontend/src/pages/year/index.ts`

**Interfaces:**
- Consumes: `t`, `getLang`, `daysShort`, `daysLong`, `fmtYearMonth`, `fmtMonthLabel`, `fmtFullDate`, `fmtCountBadge` from `utils/i18n.ts` (Task 2)

#### home/index.ts の変更

- [ ] **Step 1: home/index.ts を変更する**

`frontend/src/pages/home/index.ts` に以下の変更を加える:

**先頭に import を追加:**
```typescript
import { t, fmtYearMonth } from '../../utils/i18n';
```

**`monthLabel` 関数を置き換える（lines 22-24）:**
```typescript
// 削除:
function monthLabel(year: number, month: number): string {
  return `${year}年${month + 1}月`;
}

// 追加（ファイル先頭の import 追加で対応し、この関数は削除して使用箇所を直接 fmtYearMonth に置き換え）:
// refreshCalendar 内の label.textContent = monthLabel(...) を以下に変更:
label.textContent = fmtYearMonth(currentYear, currentMonth + 1);

// mount 内の innerHTML の ${monthLabel(...)} を以下に変更:
// <h2 class="month-label" id="month-label">${fmtYearMonth(currentYear, currentMonth + 1)}</h2>
```

**エラーメッセージを置き換える（line 30, 39）:**
```typescript
// line 30
wrapper.innerHTML = `<div class="calendar-loading">${t('common.loading')}</div>`;

// line 39
wrapper.innerHTML = `<div class="calendar-error">${t('home.error')}</div>`;
```

#### day/index.ts の変更

- [ ] **Step 2: day/index.ts を変更する**

`frontend/src/pages/day/index.ts` に以下の変更を加える:

**先頭に import を追加:**
```typescript
import { t, getLang, daysLong, fmtMonthLabel, fmtFullDate, fmtCountBadge } from '../../utils/i18n';
```

**`DAY_NAMES_JA` 定数を削除する（line 8）:**
```typescript
// 削除:
const DAY_NAMES_JA  = ['日曜日', '月曜日', '火曜日', '水曜日', '木曜日', '金曜日', '土曜日'];
```

**`headerText` 関数を置き換える（lines 41-47）:**
```typescript
function headerText(dateStr: string): { name: string; date: string } {
  const d = new Date(`${dateStr}T00:00:00`);
  return {
    name: daysLong()[d.getDay()],
    date: getLang() === 'ja'
      ? `${d.getMonth() + 1}月 ${d.getDate()}日`
      : `${fmtMonthLabel(d.getMonth() + 1)} ${d.getDate()}`,
  };
}
```

**`formatDate` 関数を削除する（lines 56-62）。使用箇所を `fmtFullDate` に置き換える:**
```typescript
// 削除:
function formatDate(dateStr: string): string { ... }

// 使用箇所（line 646）を置き換え:
// 変更前: datePara.textContent = formatDate(s.date);
// 変更後:
datePara.textContent = fmtFullDate(new Date(`${s.date}T00:00:00`));
```

**削除ボタンのテキスト（lines 235, 412）を置き換え:**
```typescript
deleteBtn.textContent = t('day.deleteBtn');
```

**読み込み中テキスト（line 611）を置き換え:**
```typescript
loadEl.textContent = t('common.loading');
```

**`handleDelete` の confirm（line 743）を置き換え:**
```typescript
if (!window.confirm(t('day.deleteConfirm'))) { closeOpenCard(); return; }
```

**データ取得エラー（line 763, 774）を置き換え:**
```typescript
area.innerHTML = `<div class="day-empty">${t('day.fetchError')}</div>`;
```

**シートエラー（line 703）を置き換え:**
```typescript
errEl.textContent = t('day.sheetError');
```

**`creatorLabel`（line 635）を置き換え:**
```typescript
const creatorLabel = s.creatorId === storedId ? t('day.self') : t('day.other');
```

**終日テキスト（line 633）を置き換え:**
```typescript
const timeStr = s.startTime
  ? `${s.startTime.slice(0, 5)}${s.endTime ? ` - ${s.endTime.slice(0, 5)}` : ''}`
  : t('common.allDay');
```

**予定なしテキスト（lines 221, 366）を置き換え:**
```typescript
area.innerHTML = `<div class="day-empty">${t('day.empty')}</div>`;
```

**オーバーフローバッジ（line 443）を置き換え:**
```typescript
badge.textContent = fmtCountBadge(overflowBadge);
```

#### week/index.ts の変更

- [ ] **Step 3: week/index.ts を変更する**

`frontend/src/pages/week/index.ts` に以下の変更を加える:

**先頭に import を追加:**
```typescript
import { daysShort, fmtMonthLabel, fmtCountBadge, t } from '../../utils/i18n';
```

**`DAY_NAMES_JA` 定数を削除し、使用箇所を置き換える（line 6, 53）:**
```typescript
// 削除:
const DAY_NAMES_JA = ['日', '月', '火', '水', '木', '金', '土'];

// 使用箇所（renderRows内 line 53）を置き換え:
const dayName = daysShort()[d.getDay()];
```

**`getMonthLabel` 関数を置き換える（lines 29-31）:**
```typescript
function getMonthLabel(dateStr: string): string {
  const d = new Date(`${dateStr}T00:00:00`);
  return fmtMonthLabel(d.getMonth() + 1);
}
```

**終日テキスト（line 104）を置き換え:**
```typescript
: t('common.allDay');
```

**バッジテキスト（line 118）を置き換え:**
```typescript
badge.textContent = fmtCountBadge(list.length - 2);
```

**週ラベル（mount 内の innerHTML line 283）を置き換え:**
```typescript
<div class="week-month-label" id="week-month-label">${getMonthLabel(startDate)}</div>
```
（`getMonthLabel` が `fmtMonthLabel` を呼ぶようになっているので自動的に i18n 対応される）

#### year/index.ts の変更

- [ ] **Step 4: year/index.ts を変更する**

`frontend/src/pages/year/index.ts` に以下の変更を加える:

**先頭に import を追加:**
```typescript
import { months, daysShort } from '../../utils/i18n';
```

**`MONTH_NAMES` と `DOW_LABELS` 定数を削除し、使用箇所を置き換える（lines 5-6）:**
```typescript
// 削除:
const MONTH_NAMES = ['1月','2月','3月','4月','5月','6月','7月','8月','9月','10月','11月','12月'];
const DOW_LABELS  = ['S','M','T','W','T','F','S'];
```

**`buildMonthCard` 内の月名（line 34）を置き換え:**
```typescript
name.textContent = months()[month];  // months() は 0-indexed で OK（配列のまま使う）
```

**`buildMonthCard` 内の曜日ヘッダー（line 40）を置き換え:**
```typescript
DOW_LABELS.forEach((label, i) => { ... })
// ↓
daysShort().forEach((label, i) => { ... })
```

- [ ] **Step 5: ビルドして確認する**

```bash
cd frontend && npm run build
```

Expected: ビルドエラーなし

- [ ] **Step 6: コミットする**

```bash
git add frontend/src/pages/home/index.ts \
        frontend/src/pages/day/index.ts \
        frontend/src/pages/week/index.ts \
        frontend/src/pages/year/index.ts
git commit -m "feat: i18n calendar pages (home, day, week, year)"
```

---

### Task 6: スケジュールページの i18n 対応（new / edit）

**Files:**
- Modify: `frontend/src/pages/schedule/new/index.ts`
- Modify: `frontend/src/pages/schedule/edit/index.ts`

**Interfaces:**
- Consumes: `t`, `getLang` from `utils/i18n.ts` (Task 2)

- [ ] **Step 1: schedule/new/index.ts を読む**

`frontend/src/pages/schedule/new/index.ts` を最初から最後まで読み、日本語ハードコード文言を全て把握する。

- [ ] **Step 2: schedule/new/index.ts を変更する**

**先頭に import を追加:**
```typescript
import { t, getLang } from '../../../utils/i18n';
```

**変更する箇所（実ファイルを読んだ上で以下の置き換えを行う）:**

| 元の文言 | 置き換え |
|---|---|
| `placeholder="タイトル"` | `placeholder="${t('schedule.label.title')}"` |
| `'ジャンルを選択'`（ラベルテキスト） | `t('schedule.genre.placeholder')` |
| `<label for="start-time">開始</label>` | `<label for="start-time">${t('schedule.label.startTime')}</label>` |
| `<label for="end-time">終了</label>` | `<label for="end-time">${t('schedule.label.endTime')}</label>` |
| `'公開'` | `t('schedule.visibility.public')` |
| `'非公開'` | `t('schedule.visibility.private')` |
| `'終日'`（toggle button） | `t('schedule.allDay')` |
| `'なし'`（通知） | `t('schedule.notif.none')` |
| `'予定時刻'` | `t('schedule.notif.atTime')` |
| `'${n}分前'` | `getLang() === 'ja' ? \`${n}分前\` : \`${n} ${t('schedule.notif.before')}\`` |
| `'📝 詳細メモを追加…'` | `t('schedule.memo.add')` |
| `'保存する'` | `t('schedule.save')` |
| `'タイトルを入力してください'` | `t('schedule.error.noTitle')` |
| `'ジャンルを選択してください'` | `t('schedule.error.noGenre')` |
| `'ジャンルがまだ作成されていません'` | `t('schedule.error.noGenreList')` |
| `'ジャンルを取得できませんでした'` | `t('schedule.error.fetchGenre')` |
| `'読み込み中…'` | `t('schedule.history.loading')` |
| `'履歴がありません'` | `t('schedule.history.empty')` |
| `'取得に失敗しました'` | `t('schedule.history.error')` |
| `'保存に失敗しました'` | `t('common.error.save')` |
| `'通信エラーが発生しました'` | `t('common.error.network')` |

タイトル部分（`<h1>` または相当要素）を `t('schedule.new.title')` に置き換える。

- [ ] **Step 3: schedule/edit/index.ts を読む**

`frontend/src/pages/schedule/edit/index.ts` を最初から最後まで読み、日本語ハードコード文言を全て把握する。

- [ ] **Step 4: schedule/edit/index.ts を変更する**

**先頭に import を追加:**
```typescript
import { t, getLang } from '../../../utils/i18n';
```

**schedule/new/index.ts と共通の文言は同じ置き換えを適用する（Step 2 の表を参照）。**

追加で以下を置き換える:

| 元の文言 | 置き換え |
|---|---|
| `'予定追加'`（タイトル） | `t('schedule.edit.title')` |
| `'削除する'` | `t('schedule.delete')` |

- [ ] **Step 5: ビルドして確認する**

```bash
cd frontend && npm run build
```

Expected: ビルドエラーなし

- [ ] **Step 6: コミットする**

```bash
git add frontend/src/pages/schedule/new/index.ts \
        frontend/src/pages/schedule/edit/index.ts
git commit -m "feat: i18n schedule pages (new, edit)"
```

---

### Task 7: ノート・メモページの i18n 対応

**Files:**
- Modify: `frontend/src/pages/notes/index.ts`
- Modify: `frontend/src/pages/notes/form/index.ts`
- Modify: `frontend/src/pages/notes/archive/index.ts`
- Modify: `frontend/src/pages/notes/memos/index.ts`
- Modify: `frontend/src/pages/notes/memos/edit/index.ts`

**Interfaces:**
- Consumes: `t` from `utils/i18n.ts` (Task 2)

- [ ] **Step 1: 各ファイルの現状を確認する**

以下の5ファイルを順番に読み、日本語ハードコード文言を全て把握する:
- `frontend/src/pages/notes/index.ts`
- `frontend/src/pages/notes/form/index.ts`
- `frontend/src/pages/notes/archive/index.ts`
- `frontend/src/pages/notes/memos/index.ts`
- `frontend/src/pages/notes/memos/edit/index.ts`

- [ ] **Step 2: notes/index.ts を変更する**

**先頭に import を追加:**
```typescript
import { t } from '../../utils/i18n';
```

**変更する箇所:**

| 元の文言 | 置き換え |
|---|---|
| `'共有事項'`（タイトル） | `t('notes.title')` |
| `'+ 新規'` | `t('notes.new')` |
| `'アーカイブ'` | `t('notes.archive')` |
| `'ノートがありません'` | `t('notes.empty')` |
| `'ノートを取得できませんでした'` | `t('notes.error')` |

- [ ] **Step 3: notes/form/index.ts を変更する**

**先頭に import を追加:**
```typescript
import { t } from '../../../utils/i18n';
```

**変更する箇所:**

| 元の文言 | 置き換え |
|---|---|
| `'ノートを作成'` | `t('notes.form.createTitle')` |
| `'ノートを編集'` | `t('notes.form.editTitle')` |
| `isEdit ? 'ノートを編集' : 'ノートを作成'` | `isEdit ? t('notes.form.editTitle') : t('notes.form.createTitle')` |
| `isEdit ? '更新する' : '作成する'`（ボタン） | `isEdit ? t('notes.form.submit.edit') : t('notes.form.submit.create')` |
| `'ノート名を入力してください'` | `t('notes.form.error.empty')` |
| `'保存に失敗しました'` | `t('notes.form.error.save')` |

- [ ] **Step 4: notes/archive/index.ts を変更する**

**先頭に import を追加:**
```typescript
import { t } from '../../../utils/i18n';
```

**変更する箇所:**

| 元の文言 | 置き換え |
|---|---|
| `'アーカイブ'`（タイトル） | `t('notes.archive.title')` |
| `'アーカイブされたノートはありません'` | `t('notes.archive.empty')` |
| `'取得できませんでした'` | `t('notes.archive.error')` |
| `'このノートを完全に削除しますか？この操作は取り消せません。'` | `t('notes.archive.deleteConfirm')` |
| `'削除に失敗しました'` | `t('notes.archive.deleteError')` |

- [ ] **Step 5: notes/memos/index.ts を変更する**

**先頭に import を追加:**
```typescript
import { t } from '../../../utils/i18n';
```

**変更する箇所:**

| 元の文言 | 置き換え |
|---|---|
| `'このノートをアーカイブに移動しますか？'` | `t('memos.archiveConfirm')` |
| `'アーカイブに失敗しました'` | `t('memos.archiveError')` |
| `'メモの作成に失敗しました'` | `t('memos.createError')` |
| `'読み込み中…'` | `t('common.loading')` |
| `'メモを取得できませんでした'` | `t('memos.error')` |
| `'メモがありません'` | `t('memos.empty')` |
| `'（タイトルなし）'` | `t('memos.noTitle')` |

- [ ] **Step 6: notes/memos/edit/index.ts を変更する**

**先頭に import を追加:**
```typescript
import { t } from '../../../../utils/i18n';
```

**変更する箇所:**

| 元の文言 | 置き換え |
|---|---|
| `'メモを取得できませんでした'` | `t('memo.edit.error')` |
| `'このメモを削除しますか？'` | `t('memo.edit.deleteConfirm')` |
| `'削除に失敗しました'` | `t('memo.edit.deleteError')` |
| `'編集中…'` | `t('memo.edit.editing')` |
| `'保存中…'` | `t('memo.edit.saving')` |
| `'保存しました'` | `t('memo.edit.saved')` |
| `'保存に失敗'` | `t('memo.edit.saveFail')` |

- [ ] **Step 7: ビルドして確認する**

```bash
cd frontend && npm run build
```

Expected: ビルドエラーなし

- [ ] **Step 8: コミットする**

```bash
git add frontend/src/pages/notes/index.ts \
        frontend/src/pages/notes/form/index.ts \
        frontend/src/pages/notes/archive/index.ts \
        frontend/src/pages/notes/memos/index.ts \
        frontend/src/pages/notes/memos/edit/index.ts
git commit -m "feat: i18n notes and memos pages"
```

---

### Task 8: 設定ページの i18n 対応（hub / password / notifications / genres）

**Files:**
- Modify: `frontend/src/pages/settings/index.ts`
- Modify: `frontend/src/pages/settings/password/index.ts`
- Modify: `frontend/src/pages/settings/notifications/index.ts`
- Modify: `frontend/src/pages/settings/genres/index.ts`
- Modify: `frontend/src/pages/settings/genres/form/index.ts`

**Interfaces:**
- Consumes: `t` from `utils/i18n.ts` (Task 2)

- [ ] **Step 1: settings/index.ts を変更する**

**先頭に import を追加:**
```typescript
import { t } from '../../utils/i18n';
```

**変更する箇所（innerHTML 内のテキスト）:**

```typescript
// タイトル
<h1 class="settings-title">${t('settings.title')}</h1>

// メニュー項目ラベル
<span class="settings-menu-label">${t('settings.menu.profile')}</span>
<span class="settings-menu-label">${t('settings.menu.notifications')}</span>
<span class="settings-menu-label">${t('settings.menu.genres')}</span>
<span class="settings-menu-label">${t('settings.menu.password')}</span>
// admin 項目（isAdmin の場合）:
<span class="settings-menu-label">${t('settings.menu.admin')}</span>
```

- [ ] **Step 2: settings/password/index.ts を変更する**

**先頭に import を追加:**
```typescript
import { t } from '../../../utils/i18n';
```

**変更する箇所:**

| 元の文言 | 置き換え |
|---|---|
| `'パスワード設定'`（タイトル） | `t('password.title')` |
| `placeholder="古いパスワードを入力"` | `placeholder="${t('password.placeholder.current')}"` |
| `placeholder="新しいパスワードを入力"` | `placeholder="${t('password.placeholder.new')}"` |
| `placeholder="確認用"` | `placeholder="${t('password.placeholder.confirm')}"` |
| `'設定する'`（ボタン） | `t('password.save')` |
| `'すべてのフィールドを入力してください'` | `t('password.error.empty')` |
| `'新しいパスワードと確認用が一致しません'` | `t('password.error.confirmMismatch')` |
| `'パスワードを変更しました'`（alert） | `t('password.saved')` |
| `'現在のパスワードが正しくありません'` | `t('password.error.mismatch')` |
| `'エラーが発生しました'` | `t('password.error.generic')` |
| `'通信エラーが発生しました'` | `t('common.error.network')` |

- [ ] **Step 3: settings/notifications/index.ts を読み変更する**

`frontend/src/pages/settings/notifications/index.ts` を読み、全文を把握した上で変更する。

**先頭に import を追加:**
```typescript
import { t } from '../../../utils/i18n';
```

**変更する箇所:**

| 元の文言 | 置き換え |
|---|---|
| `'通知設定'`（タイトル） | `t('notif.title')` |
| `'読み込み中…'` | `t('common.loading')` |
| `'ジャンルがまだ作成されていません'` | `t('notif.empty')` |
| `'設定の取得に失敗しました'`（存在する場合） | `t('notif.error')` |

- [ ] **Step 4: settings/genres/index.ts を変更する**

**先頭に import を追加:**
```typescript
import { t } from '../../../utils/i18n';
```

**変更する箇所:**

| 元の文言 | 置き換え |
|---|---|
| `'ジャンル一覧'`（タイトル） | `t('genres.title')` |
| `'+ 新規追加する'` | `t('genres.add')` |
| `'ジャンルがまだ作成されていません'` | `t('genres.empty')` |
| `'データの取得に失敗しました'` | `t('genres.error')` |
| `'読み込み中…'` | `t('common.loading')` |

- [ ] **Step 5: settings/genres/form/index.ts を読み変更する**

`frontend/src/pages/settings/genres/form/index.ts` を最初から最後まで読み、全文を把握した上で変更する。

**先頭に import を追加:**
```typescript
import { t } from '../../../../utils/i18n';
```

**変更する箇所:**

| 元の文言 | 置き換え |
|---|---|
| `isEdit ? 'ジャンル編集' : 'ジャンル作成'`（タイトル） | `isEdit ? t('genres.form.editTitle') : t('genres.form.createTitle')` |
| `'読み込み中…'` | `t('common.loading')` |
| `isEdit ? '更新する' : '作成する'`（submit ボタン） | `isEdit ? t('genres.form.submit.edit') : t('genres.form.submit.create')` |
| `'削除する'` | `t('genres.form.delete')` |
| `'ジャンル名を入力してください'` | `t('genres.form.error.noName')` |
| `'カラーを選択してください'` | `t('genres.form.error.noColor')` |
| `'このジャンル名はすでに使われています'` | `t('genres.form.error.conflict')` |
| `'保存に失敗しました'` | `t('genres.form.error.save')` |
| `'削除に失敗しました'` | `t('genres.form.error.delete')` |
| `'このジャンルを削除しますか？'` | `t('genres.form.deleteConfirm')` |
| `'取得に失敗しました'` | `t('genres.form.fetchError')` |
| `'通信エラーが発生しました'` | `t('common.error.network')` |

ジャンル名の input の label/placeholder も対応する:
- ジャンル名ラベル → `t('genres.form.label.name')`
- カラーラベル → `t('genres.form.label.color')`

- [ ] **Step 6: ビルドして確認する**

```bash
cd frontend && npm run build
```

Expected: ビルドエラーなし

- [ ] **Step 7: コミットする**

```bash
git add frontend/src/pages/settings/index.ts \
        frontend/src/pages/settings/password/index.ts \
        frontend/src/pages/settings/notifications/index.ts \
        frontend/src/pages/settings/genres/index.ts \
        frontend/src/pages/settings/genres/form/index.ts
git commit -m "feat: i18n settings pages (hub, password, notifications, genres)"
```

---

### Task 9: 管理者ページの i18n 対応

**Files:**
- Modify: `frontend/src/pages/settings/admin/index.ts`
- Modify: `frontend/src/pages/settings/admin/users/index.ts`
- Modify: `frontend/src/pages/settings/admin/users/form/index.ts`
- Modify: `frontend/src/pages/settings/admin/groups/form/index.ts`

**Interfaces:**
- Consumes: `t`, `getLang` from `utils/i18n.ts` (Task 2)

- [ ] **Step 1: 4ファイルを読んで現状を把握する**

以下のファイルを順番に読む:
- `frontend/src/pages/settings/admin/index.ts`
- `frontend/src/pages/settings/admin/users/index.ts`
- `frontend/src/pages/settings/admin/users/form/index.ts`
- `frontend/src/pages/settings/admin/groups/form/index.ts`

- [ ] **Step 2: admin/index.ts を変更する**

**先頭に import を追加:**
```typescript
import { t } from '../../../utils/i18n';
```

**変更する箇所:**

| 元の文言 | 置き換え |
|---|---|
| `'管理者設定'`（タイトル） | `t('admin.title')` |
| `'ユーザー管理'`（メニュー） | `t('admin.menu.users')` |
| `'グループ作成'`（メニュー） | `t('admin.menu.groups')` |

- [ ] **Step 3: admin/users/index.ts を変更する**

**先頭に import を追加:**
```typescript
import { t, getLang } from '../../../../utils/i18n';
```

**変更する箇所:**

| 元の文言 | 置き換え |
|---|---|
| `'ユーザー管理'`（タイトル） | `t('admin.users.title')` |
| `'＋ 新規'` | `t('admin.users.new')` |
| `'ユーザーがいません'` | `t('admin.users.empty')` |
| `'読み込み中…'` | `t('common.loading')` |
| `'読み込みに失敗しました'` | `t('admin.users.error')` |
| `'無効化'`（ボタン） | `t('admin.users.deactivate')` |
| `'無効化済み'` | `t('admin.users.deactivated')` |
| `'無効化に失敗しました'`（alert） | `t('admin.users.deactivateError')` |
| `'管理者'`（ロールバッジ） | `t('admin.users.role.admin')` |
| `'GL'` | `t('admin.users.role.gl')` |
| `'一般'` | `t('admin.users.role.general')` |

confirm ダイアログ（`${u.name}を無効化しますか？`）を置き換え:
```typescript
const msg = getLang() === 'ja'
  ? `${u.name}を無効化しますか？`
  : `Deactivate ${u.name}?`;
if (!confirm(msg)) return;
```

- [ ] **Step 4: admin/users/form/index.ts を変更する**

**先頭に import を追加:**
```typescript
import { t } from '../../../../utils/i18n';
```

**変更する箇所:**

| 元の文言 | 置き換え |
|---|---|
| `'アカウント作成'`（タイトル） | `t('admin.user.form.title')` |
| `'名前'`（ラベル） | `t('admin.user.form.name')` |
| `'初期パスワード'`（ラベル） | `t('admin.user.form.password')` |
| `'ロール'`（ラベル） | `t('admin.user.form.role')` |
| `'管理者'`（ラジオ） | `t('admin.users.role.admin')` |
| `'GL'`（ラジオ） | `t('admin.users.role.gl')` |
| `'一般'`（ラジオ） | `t('admin.users.role.general')` |
| `'作成する'`（ボタン） | `t('admin.user.form.submit')` |
| `'すべての項目を入力してください'` | `t('admin.user.form.error.required')` |
| `'このLoginIdはすでに使われています'` | `t('admin.user.form.error.loginIdConflict')` |
| `'作成に失敗しました'` | `t('admin.user.form.error.generic')` |

- [ ] **Step 5: admin/groups/form/index.ts を変更する**

**先頭に import を追加:**
```typescript
import { t } from '../../../../utils/i18n';
```

**変更する箇所:**

| 元の文言 | 置き換え |
|---|---|
| `'グループ作成'`（タイトル） | `t('admin.group.form.title')` |
| `'グループ名'`（ラベル） | `t('admin.group.form.name')` |
| `'メンバー選択'`（ラベル） | `t('admin.group.form.members')` |
| `'グループ未所属のユーザーがいません'` | `t('admin.group.form.noUsers')` |
| `'作成する'`（ボタン） | `t('admin.group.form.submit')` |
| `'グループ名を入力してください'` | `t('admin.group.form.error.noName')` |
| `'メンバーを1人以上選択してください'` | `t('admin.group.form.error.noMember')` |
| `'作成に失敗しました'` | `t('admin.group.form.error.save')` |
| `'読み込み中…'` | `t('common.loading')` |

ロールバッジ（グループ形成時のユーザー一覧に表示）:
| 元の文言 | 置き換え |
|---|---|
| `'管理者'` | `t('admin.users.role.admin')` |
| `'GL'` | `t('admin.users.role.gl')` |
| `'一般'` | `t('admin.users.role.general')` |

- [ ] **Step 6: ビルドして確認する**

```bash
cd frontend && npm run build
```

Expected: ビルドエラーなし

- [ ] **Step 7: コミットする**

```bash
git add frontend/src/pages/settings/admin/index.ts \
        frontend/src/pages/settings/admin/users/index.ts \
        frontend/src/pages/settings/admin/users/form/index.ts \
        frontend/src/pages/settings/admin/groups/form/index.ts
git commit -m "feat: i18n admin pages"
```
