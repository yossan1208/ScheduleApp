# ログイン機能 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** SCR-00（ログイン画面）を実装し、JWT + DB セッションによる認証基盤を構築する。

**Architecture:** POST /api/auth/login でパスワード検証・JWT 発行・sessions テーブルへの保存を行い、httpOnly Cookie でトークンを管理する。全認証済みリクエストで SessionMiddleware がセッション延長（スライディングウィンドウ14日）を担当する。フロントは Vite + バニラ TS で login ページを描画し、ログイン成功後に CSS 変数へテーマカラーをセットする。

**Tech Stack:** ASP.NET Core 10 / EF Core 10 / SQL Server 2022 / BCrypt.Net-Next / System.IdentityModel.Tokens.Jwt / Vite 8 / TypeScript 6 / xUnit / Moq

---

## ファイルマップ

| 状態 | パス | 責務 |
|---|---|---|
| 新規 | `backend/ScheduleApp.Api/Models/Dtos/LoginRequest.cs` | ログインリクエスト型 |
| 新規 | `backend/ScheduleApp.Api/Models/Dtos/LoginResponse.cs` | ログインレスポンス型 |
| 新規 | `backend/ScheduleApp.Api/Models/Dtos/LoginResult.cs` | AuthService の内部戻り値 |
| 新規 | `backend/ScheduleApp.Api/Repositories/IUserRepository.cs` | ユーザー取得インターフェース |
| 新規 | `backend/ScheduleApp.Api/Repositories/UserRepository.cs` | EF Core 実装 |
| 新規 | `backend/ScheduleApp.Api/Repositories/ISessionRepository.cs` | セッション CRUD インターフェース |
| 新規 | `backend/ScheduleApp.Api/Repositories/SessionRepository.cs` | EF Core 実装 |
| 新規 | `backend/ScheduleApp.Api/Services/IAuthService.cs` | 認証サービスインターフェース |
| 新規 | `backend/ScheduleApp.Api/Services/AuthService.cs` | ログイン・ログアウトロジック |
| 新規 | `backend/ScheduleApp.Api/Controllers/AuthController.cs` | POST /api/auth/login・logout |
| 新規 | `backend/ScheduleApp.Api/Middleware/SessionMiddleware.cs` | セッション検証・延長 |
| 新規 | `backend/ScheduleApp.Tests/ScheduleApp.Tests.csproj` | テストプロジェクト |
| 新規 | `backend/ScheduleApp.Tests/Services/AuthServiceTests.cs` | AuthService 単体テスト |
| 変更 | `backend/ScheduleApp.Api/Models/Entities/User.cs` | GroupId → int?、ThemeColor ナビゲーション追加 |
| 変更 | `backend/ScheduleApp.Api/Models/Entities/Note.cs` | IsSystem 追加 |
| 変更 | `backend/ScheduleApp.Api/Data/AppDbContext.cs` | User.ThemeColor リレーション設定 |
| 変更 | `backend/ScheduleApp.Api/Program.cs` | DI 登録・ミドルウェア追加 |
| 変更 | `backend/ScheduleApp.Api/appsettings.json` | Jwt セクション追加 |
| 変更 | `backend/ScheduleApp.Api/appsettings.Development.json` | Jwt:Secret 追加 |
| 新規 | `frontend/vite.config.ts` | /api プロキシ設定 |
| 新規 | `frontend/src/styles/main.css` | CSS 変数・リセット |
| 新規 | `frontend/src/api/auth.ts` | login() / logout() |
| 新規 | `frontend/src/pages/login/index.ts` | mount() |
| 新規 | `frontend/src/pages/login/login.css` | ログインページレイアウト |
| 変更 | `frontend/src/utils/router.ts` | '/' ルート追加 |
| 変更 | `frontend/index.html` | main.css リンク追加 |

---

## Task 1: NuGet パッケージ追加

**Files:**
- Modify: `backend/ScheduleApp.Api/ScheduleApp.Api.csproj`

- [ ] **Step 1: BCrypt と JWT パッケージを追加する**

```bash
cd /Users/yoshizawayuki/ScheduleApp/backend/ScheduleApp.Api
dotnet add package BCrypt.Net-Next
dotnet add package System.IdentityModel.Tokens.Jwt
```

- [ ] **Step 2: ビルドして通ることを確認する**

```bash
dotnet build
```

期待結果: `Build succeeded`

---

## Task 2: テストプロジェクトのセットアップ

**Files:**
- Create: `backend/ScheduleApp.Tests/ScheduleApp.Tests.csproj`

- [ ] **Step 1: ソリューションとテストプロジェクトを作成する**

```bash
cd /Users/yoshizawayuki/ScheduleApp/backend
dotnet new sln -n ScheduleApp
dotnet sln add ScheduleApp.Api/ScheduleApp.Api.csproj
dotnet new xunit -n ScheduleApp.Tests -o ScheduleApp.Tests
dotnet sln add ScheduleApp.Tests/ScheduleApp.Tests.csproj
dotnet add ScheduleApp.Tests/ScheduleApp.Tests.csproj reference ScheduleApp.Api/ScheduleApp.Api.csproj
```

- [ ] **Step 2: テスト用パッケージを追加する**

```bash
cd /Users/yoshizawayuki/ScheduleApp/backend/ScheduleApp.Tests
dotnet add package Moq
dotnet add package BCrypt.Net-Next
```

- [ ] **Step 3: デフォルトのテストファイルを削除する**

```bash
rm /Users/yoshizawayuki/ScheduleApp/backend/ScheduleApp.Tests/UnitTest1.cs
```

- [ ] **Step 4: ビルドして通ることを確認する**

```bash
cd /Users/yoshizawayuki/ScheduleApp/backend
dotnet build
```

期待結果: `Build succeeded`

---

## Task 3: エンティティの更新

**Files:**
- Modify: `backend/ScheduleApp.Api/Models/Entities/User.cs`
- Modify: `backend/ScheduleApp.Api/Models/Entities/Note.cs`

- [ ] **Step 1: User.cs を更新する（GroupId を nullable に、ThemeColor ナビゲーション追加）**

`backend/ScheduleApp.Api/Models/Entities/User.cs` を以下の内容に書き換える:

```csharp
namespace ScheduleApp.Api.Models.Entities;

public class User
{
    public int    Id              { get; set; }
    public string LoginId         { get; set; } = string.Empty;
    public string Name            { get; set; } = string.Empty;
    public string PasswordHash    { get; set; } = string.Empty;
    public int    PersonalColorId { get; set; }
    public int    ThemeColorId    { get; set; }
    public short  Role            { get; set; }
    public int?   GroupId         { get; set; }
    public bool   IsActive        { get; set; } = true;

    public Color? ThemeColor { get; set; }
}
```

- [ ] **Step 2: Note.cs に IsSystem を追加する**

`backend/ScheduleApp.Api/Models/Entities/Note.cs` を以下の内容に書き換える:

```csharp
namespace ScheduleApp.Api.Models.Entities;

public class Note
{
    public int       Id         { get; set; }
    public string?   Name       { get; set; }
    public string?   Color      { get; set; }
    public int?      GroupId    { get; set; }
    public int       CreatorId  { get; set; }
    public DateTime? CreatedAt  { get; set; }
    public bool      IsArchived { get; set; } = false;
    public bool      IsSystem   { get; set; } = false;
    public int?      UpdatedBy  { get; set; }
    public DateTime? UpdatedAt  { get; set; }
}
```

---

## Task 4: AppDbContext の更新

**Files:**
- Modify: `backend/ScheduleApp.Api/Data/AppDbContext.cs`

- [ ] **Step 1: User.ThemeColor のリレーションを設定する**

`AppDbContext.cs` の `OnModelCreating` 内の User 設定を以下に変更する:

```csharp
modelBuilder.Entity<User>(e =>
{
    e.ToTable("users");
    e.HasIndex(x => x.LoginId).IsUnique();
    e.Property(x => x.LoginId).HasMaxLength(50);
    e.HasOne(x => x.ThemeColor)
     .WithMany()
     .HasForeignKey(x => x.ThemeColorId)
     .OnDelete(DeleteBehavior.Restrict);
});
```

---

## Task 5: EF Core マイグレーション

**Files:**
- Create: `backend/ScheduleApp.Api/Migrations/` (自動生成)

- [ ] **Step 1: マイグレーションを作成する**

```bash
cd /Users/yoshizawayuki/ScheduleApp/backend/ScheduleApp.Api
dotnet ef migrations add AddGroupIdNullableAndNoteIsSystem
```

期待結果: `Done. To undo this action, use 'ef migrations remove'`

- [ ] **Step 2: マイグレーションを適用する**

```bash
dotnet ef database update
```

期待結果: `Done.`

---

## Task 6: DTO の作成

**Files:**
- Create: `backend/ScheduleApp.Api/Models/Dtos/LoginRequest.cs`
- Create: `backend/ScheduleApp.Api/Models/Dtos/LoginResponse.cs`
- Create: `backend/ScheduleApp.Api/Models/Dtos/LoginResult.cs`

- [ ] **Step 1: LoginRequest.cs を作成する**

```csharp
namespace ScheduleApp.Api.Models.Dtos;

public class LoginRequest
{
    public string LoginId  { get; set; } = string.Empty;
    public string Password { get; set; } = string.Empty;
}
```

- [ ] **Step 2: LoginResponse.cs を作成する**

```csharp
namespace ScheduleApp.Api.Models.Dtos;

public class LoginResponse
{
    public int    UserId        { get; set; }
    public string Name          { get; set; } = string.Empty;
    public short  Role          { get; set; }
    public string ThemeColorHex { get; set; } = string.Empty;
}
```

- [ ] **Step 3: LoginResult.cs を作成する**

```csharp
namespace ScheduleApp.Api.Models.Dtos;

public record LoginResult(LoginResponse? Response, string? Token, string? ErrorCode);
```

---

## Task 7: UserRepository の作成

**Files:**
- Create: `backend/ScheduleApp.Api/Repositories/IUserRepository.cs`
- Create: `backend/ScheduleApp.Api/Repositories/UserRepository.cs`

- [ ] **Step 1: IUserRepository.cs を作成する**

```csharp
using ScheduleApp.Api.Models.Entities;

namespace ScheduleApp.Api.Repositories;

public interface IUserRepository
{
    Task<User?> FindByLoginIdAsync(string loginId);
}
```

- [ ] **Step 2: UserRepository.cs を作成する**

```csharp
using Microsoft.EntityFrameworkCore;
using ScheduleApp.Api.Data;
using ScheduleApp.Api.Models.Entities;

namespace ScheduleApp.Api.Repositories;

public class UserRepository(AppDbContext db) : IUserRepository
{
    public Task<User?> FindByLoginIdAsync(string loginId)
        => db.Users
             .Include(u => u.ThemeColor)
             .FirstOrDefaultAsync(u => u.LoginId == loginId);
}
```

---

## Task 8: SessionRepository の作成

**Files:**
- Create: `backend/ScheduleApp.Api/Repositories/ISessionRepository.cs`
- Create: `backend/ScheduleApp.Api/Repositories/SessionRepository.cs`

- [ ] **Step 1: ISessionRepository.cs を作成する**

```csharp
using ScheduleApp.Api.Models.Entities;

namespace ScheduleApp.Api.Repositories;

public interface ISessionRepository
{
    Task CreateAsync(Session session);
    Task<Session?> FindByTokenAsync(string token);
    Task DeleteAsync(int sessionId);
    Task UpdateLastActiveAsync(int sessionId, DateTime expiresAt, DateTime lastActiveAt);
}
```

- [ ] **Step 2: SessionRepository.cs を作成する**

```csharp
using Microsoft.EntityFrameworkCore;
using ScheduleApp.Api.Data;
using ScheduleApp.Api.Models.Entities;

namespace ScheduleApp.Api.Repositories;

public class SessionRepository(AppDbContext db) : ISessionRepository
{
    public async Task CreateAsync(Session session)
    {
        db.Sessions.Add(session);
        await db.SaveChangesAsync();
    }

    public Task<Session?> FindByTokenAsync(string token)
        => db.Sessions.FirstOrDefaultAsync(s => s.Token == token);

    public async Task DeleteAsync(int sessionId)
    {
        var session = await db.Sessions.FindAsync(sessionId);
        if (session != null)
        {
            db.Sessions.Remove(session);
            await db.SaveChangesAsync();
        }
    }

    public async Task UpdateLastActiveAsync(int sessionId, DateTime expiresAt, DateTime lastActiveAt)
    {
        await db.Sessions
            .Where(s => s.Id == sessionId)
            .ExecuteUpdateAsync(s => s
                .SetProperty(x => x.ExpiresAt,    expiresAt)
                .SetProperty(x => x.LastActiveAt, lastActiveAt));
    }
}
```

---

## Task 9: AuthService の失敗テストを書く

**Files:**
- Create: `backend/ScheduleApp.Tests/Services/AuthServiceTests.cs`

- [ ] **Step 1: テストファイルを作成する**

`backend/ScheduleApp.Tests/Services/AuthServiceTests.cs`:

```csharp
using Microsoft.Extensions.Configuration;
using Moq;
using ScheduleApp.Api.Models.Dtos;
using ScheduleApp.Api.Models.Entities;
using ScheduleApp.Api.Repositories;
using ScheduleApp.Api.Services;

namespace ScheduleApp.Tests.Services;

public class AuthServiceTests
{
    private readonly Mock<IUserRepository>    _userRepo    = new();
    private readonly Mock<ISessionRepository> _sessionRepo = new();
    private readonly IAuthService             _sut;

    public AuthServiceTests()
    {
        var config = new ConfigurationBuilder()
            .AddInMemoryCollection(new Dictionary<string, string?>
            {
                { "Jwt:Secret", "TestSecretKey_MustBe32CharsOrMore_ForHmacSha256!" },
            })
            .Build();
        _sut = new AuthService(_userRepo.Object, _sessionRepo.Object, config);
    }

    [Fact]
    public async Task LoginAsync_UserNotFound_ReturnsInvalidCredentials()
    {
        _userRepo.Setup(r => r.FindByLoginIdAsync("unknown")).ReturnsAsync((User?)null);

        var result = await _sut.LoginAsync(new LoginRequest { LoginId = "unknown", Password = "any" });

        Assert.Equal("AUTH_INVALID_CREDENTIALS", result.ErrorCode);
        Assert.Null(result.Response);
        Assert.Null(result.Token);
    }

    [Fact]
    public async Task LoginAsync_WrongPassword_ReturnsInvalidCredentials()
    {
        var user = new User
        {
            LoginId      = "user1",
            PasswordHash = BCrypt.Net.BCrypt.HashPassword("correctpass"),
            IsActive     = true,
            GroupId      = 1,
        };
        _userRepo.Setup(r => r.FindByLoginIdAsync("user1")).ReturnsAsync(user);

        var result = await _sut.LoginAsync(new LoginRequest { LoginId = "user1", Password = "wrongpass" });

        Assert.Equal("AUTH_INVALID_CREDENTIALS", result.ErrorCode);
    }

    [Fact]
    public async Task LoginAsync_InactiveUser_ReturnsAccountDisabled()
    {
        var user = new User
        {
            LoginId      = "user1",
            PasswordHash = BCrypt.Net.BCrypt.HashPassword("pass"),
            IsActive     = false,
            GroupId      = 1,
        };
        _userRepo.Setup(r => r.FindByLoginIdAsync("user1")).ReturnsAsync(user);

        var result = await _sut.LoginAsync(new LoginRequest { LoginId = "user1", Password = "pass" });

        Assert.Equal("AUTH_ACCOUNT_DISABLED", result.ErrorCode);
    }

    [Fact]
    public async Task LoginAsync_NullGroupId_ReturnsNoGroup()
    {
        var user = new User
        {
            LoginId      = "user1",
            PasswordHash = BCrypt.Net.BCrypt.HashPassword("pass"),
            IsActive     = true,
            GroupId      = null,
        };
        _userRepo.Setup(r => r.FindByLoginIdAsync("user1")).ReturnsAsync(user);

        var result = await _sut.LoginAsync(new LoginRequest { LoginId = "user1", Password = "pass" });

        Assert.Equal("AUTH_NO_GROUP", result.ErrorCode);
    }

    [Fact]
    public async Task LoginAsync_ValidCredentials_ReturnsTokenAndResponse()
    {
        var user = new User
        {
            Id           = 1,
            LoginId      = "user1",
            Name         = "山田太郎",
            PasswordHash = BCrypt.Net.BCrypt.HashPassword("pass"),
            IsActive     = true,
            GroupId      = 5,
            Role         = 2,
            ThemeColor   = new Color { HexCode = "#FF8C00" },
        };
        _userRepo.Setup(r => r.FindByLoginIdAsync("user1")).ReturnsAsync(user);
        _sessionRepo.Setup(r => r.CreateAsync(It.IsAny<Session>())).Returns(Task.CompletedTask);

        var result = await _sut.LoginAsync(new LoginRequest { LoginId = "user1", Password = "pass" });

        Assert.Null(result.ErrorCode);
        Assert.NotNull(result.Token);
        Assert.NotNull(result.Response);
        Assert.Equal("山田太郎", result.Response.Name);
        Assert.Equal("#FF8C00", result.Response.ThemeColorHex);
    }
}
```

- [ ] **Step 2: テストを実行して全件 FAIL を確認する**

```bash
cd /Users/yoshizawayuki/ScheduleApp/backend
dotnet test --filter "FullyQualifiedName~AuthServiceTests"
```

期待結果: `AuthService` が存在しないためビルドエラーまたは全テスト FAIL

---

## Task 10: AuthService の実装

**Files:**
- Create: `backend/ScheduleApp.Api/Services/IAuthService.cs`
- Create: `backend/ScheduleApp.Api/Services/AuthService.cs`

- [ ] **Step 1: IAuthService.cs を作成する**

```csharp
using ScheduleApp.Api.Models.Dtos;

namespace ScheduleApp.Api.Services;

public interface IAuthService
{
    Task<LoginResult> LoginAsync(LoginRequest request);
    Task LogoutAsync(string token);
}
```

- [ ] **Step 2: AuthService.cs を作成する**

```csharp
using System.IdentityModel.Tokens.Jwt;
using System.Security.Claims;
using System.Text;
using Microsoft.IdentityModel.Tokens;
using ScheduleApp.Api.Models.Dtos;
using ScheduleApp.Api.Models.Entities;
using ScheduleApp.Api.Repositories;

namespace ScheduleApp.Api.Services;

public class AuthService(
    IUserRepository    userRepo,
    ISessionRepository sessionRepo,
    IConfiguration     config) : IAuthService
{
    public async Task<LoginResult> LoginAsync(LoginRequest request)
    {
        var user = await userRepo.FindByLoginIdAsync(request.LoginId);

        if (user == null || !BCrypt.Net.BCrypt.Verify(request.Password, user.PasswordHash))
            return new LoginResult(null, null, "AUTH_INVALID_CREDENTIALS");

        if (!user.IsActive)
            return new LoginResult(null, null, "AUTH_ACCOUNT_DISABLED");

        if (user.GroupId == null)
            return new LoginResult(null, null, "AUTH_NO_GROUP");

        var token = GenerateJwt(user);

        await sessionRepo.CreateAsync(new Session
        {
            UserId       = user.Id,
            Token        = token,
            ExpiresAt    = DateTime.UtcNow.AddDays(14),
            LastActiveAt = DateTime.UtcNow,
        });

        var response = new LoginResponse
        {
            UserId        = user.Id,
            Name          = user.Name,
            Role          = user.Role,
            ThemeColorHex = user.ThemeColor?.HexCode ?? "#000000",
        };

        return new LoginResult(response, token, null);
    }

    public async Task LogoutAsync(string token)
    {
        var session = await sessionRepo.FindByTokenAsync(token);
        if (session != null)
            await sessionRepo.DeleteAsync(session.Id);
    }

    private string GenerateJwt(User user)
    {
        var key = new SymmetricSecurityKey(Encoding.UTF8.GetBytes(config["Jwt:Secret"]!));
        var descriptor = new SecurityTokenDescriptor
        {
            Subject = new ClaimsIdentity(
            [
                new Claim("userId",  user.Id.ToString()),
                new Claim("role",    user.Role.ToString()),
                new Claim("groupId", user.GroupId!.Value.ToString()),
            ]),
            Expires           = DateTime.UtcNow.AddDays(14),
            SigningCredentials = new SigningCredentials(key, SecurityAlgorithms.HmacSha256Signature),
        };
        var handler = new JwtSecurityTokenHandler();
        return handler.WriteToken(handler.CreateToken(descriptor));
    }
}
```

---

## Task 11: テストを実行して全件 PASS を確認する

- [ ] **Step 1: テストを実行する**

```bash
cd /Users/yoshizawayuki/ScheduleApp/backend
dotnet test --filter "FullyQualifiedName~AuthServiceTests" -v normal
```

期待結果:

```
Passed: LoginAsync_UserNotFound_ReturnsInvalidCredentials
Passed: LoginAsync_WrongPassword_ReturnsInvalidCredentials
Passed: LoginAsync_InactiveUser_ReturnsAccountDisabled
Passed: LoginAsync_NullGroupId_ReturnsNoGroup
Passed: LoginAsync_ValidCredentials_ReturnsTokenAndResponse

Test Run Successful.
```

- [ ] **Step 2: ここで一度コミットする**

```bash
cd /Users/yoshizawayuki/ScheduleApp
git add backend/
git commit -m "feat: add AuthService with login/logout logic and tests"
```

---

## Task 12: AuthController の作成

**Files:**
- Create: `backend/ScheduleApp.Api/Controllers/AuthController.cs`

- [ ] **Step 1: AuthController.cs を作成する**

```csharp
using Microsoft.AspNetCore.Mvc;
using ScheduleApp.Api.Models;
using ScheduleApp.Api.Models.Dtos;
using ScheduleApp.Api.Services;

namespace ScheduleApp.Api.Controllers;

[ApiController]
[Route("api/auth")]
public class AuthController(IAuthService authService) : ControllerBase
{
    private static readonly Dictionary<string, string> ErrorMessages = new()
    {
        { "AUTH_INVALID_CREDENTIALS", "IDまたはパスワードが違います" },
        { "AUTH_ACCOUNT_DISABLED",    "アカウントが無効です" },
        { "AUTH_NO_GROUP",            "グループに加入してください" },
    };

    [HttpPost("login")]
    public async Task<IActionResult> Login([FromBody] LoginRequest request)
    {
        var result = await authService.LoginAsync(request);

        if (result.ErrorCode != null)
        {
            return Ok(new ApiEnvelope<object>
            {
                Success = false,
                Error   = new ApiError(result.ErrorCode, ErrorMessages[result.ErrorCode]),
            });
        }

        Response.Cookies.Append("jwt", result.Token!, new CookieOptions
        {
            HttpOnly  = true,
            Secure    = false, // 開発環境は http なので false
            SameSite  = SameSiteMode.Strict,
            Expires   = DateTimeOffset.UtcNow.AddDays(14),
        });

        return Ok(new ApiEnvelope<LoginResponse>
        {
            Success = true,
            Data    = result.Response,
        });
    }

    [HttpPost("logout")]
    public async Task<IActionResult> Logout()
    {
        var token = Request.Cookies["jwt"];
        if (token != null)
            await authService.LogoutAsync(token);

        Response.Cookies.Delete("jwt");

        return Ok(new ApiEnvelope<object> { Success = true });
    }
}
```

---

## Task 13: SessionMiddleware の作成

**Files:**
- Create: `backend/ScheduleApp.Api/Middleware/SessionMiddleware.cs`

- [ ] **Step 1: SessionMiddleware.cs を作成する**

```csharp
using System.IdentityModel.Tokens.Jwt;
using ScheduleApp.Api.Models;
using ScheduleApp.Api.Repositories;

namespace ScheduleApp.Api.Middleware;

public class SessionMiddleware(RequestDelegate next, IServiceScopeFactory scopeFactory)
{
    public async Task InvokeAsync(HttpContext context)
    {
        // 認証エンドポイントはミドルウェアをスキップ
        if (context.Request.Path.StartsWithSegments("/api/auth"))
        {
            await next(context);
            return;
        }

        var token = context.Request.Cookies["jwt"];
        if (token == null)
        {
            context.Response.StatusCode = 401;
            await context.Response.WriteAsJsonAsync(new ApiEnvelope<object>
            {
                Success = false,
                Error   = new ApiError("AUTH_REQUIRED", "ログインが必要です"),
            });
            return;
        }

        using var scope = scopeFactory.CreateScope();
        var sessionRepo = scope.ServiceProvider.GetRequiredService<ISessionRepository>();

        var session = await sessionRepo.FindByTokenAsync(token);
        if (session == null || session.ExpiresAt < DateTime.UtcNow)
        {
            if (session != null) await sessionRepo.DeleteAsync(session.Id);
            context.Response.Cookies.Delete("jwt");
            context.Response.StatusCode = 401;
            await context.Response.WriteAsJsonAsync(new ApiEnvelope<object>
            {
                Success = false,
                Error   = new ApiError("SESSION_EXPIRED", "セッションが期限切れです"),
            });
            return;
        }

        var now = DateTime.UtcNow;
        await sessionRepo.UpdateLastActiveAsync(session.Id, now.AddDays(14), now);

        var handler = new JwtSecurityTokenHandler();
        var jwt     = handler.ReadJwtToken(token);
        context.Items["userId"]  = int.Parse(jwt.Claims.First(c => c.Type == "userId").Value);
        context.Items["role"]    = short.Parse(jwt.Claims.First(c => c.Type == "role").Value);
        context.Items["groupId"] = int.Parse(jwt.Claims.First(c => c.Type == "groupId").Value);

        await next(context);
    }
}
```

---

## Task 14: Program.cs と appsettings の更新

**Files:**
- Modify: `backend/ScheduleApp.Api/Program.cs`
- Modify: `backend/ScheduleApp.Api/appsettings.json`
- Modify: `backend/ScheduleApp.Api/appsettings.Development.json`

- [ ] **Step 1: Program.cs を書き換える**

```csharp
using ScheduleApp.Api.Data;
using ScheduleApp.Api.Middleware;
using ScheduleApp.Api.Repositories;
using ScheduleApp.Api.Services;
using Microsoft.EntityFrameworkCore;

var builder = WebApplication.CreateBuilder(args);

builder.Services.AddControllers();
builder.Services.AddOpenApi();

builder.Services.AddDbContext<AppDbContext>(options =>
    options.UseSqlServer(builder.Configuration.GetConnectionString("DefaultConnection")));

builder.Services.AddScoped<IUserRepository,    UserRepository>();
builder.Services.AddScoped<ISessionRepository, SessionRepository>();
builder.Services.AddScoped<IAuthService,       AuthService>();

builder.Services.AddCors(options =>
{
    options.AddDefaultPolicy(policy =>
        policy.WithOrigins("http://localhost:5173")
              .AllowAnyHeader()
              .AllowAnyMethod()
              .AllowCredentials());
});

var app = builder.Build();

if (app.Environment.IsDevelopment())
    app.MapOpenApi();

app.UseCors();
app.UseMiddleware<SessionMiddleware>();
app.MapControllers();

app.Run();
```

- [ ] **Step 2: appsettings.json に Jwt セクションを追加する**

`appsettings.json` の末尾の `}` の前に追加する:

```json
{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "Jwt": {
    "Secret": "REPLACE_IN_DEVELOPMENT_JSON"
  }
}
```

- [ ] **Step 3: appsettings.Development.json に Jwt:Secret を追加する**

```json
{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "ConnectionStrings": {
    "DefaultConnection": "Server=localhost,1433;Database=ScheduleApp;User Id=sa;Password=ScheduleApp_2024!;TrustServerCertificate=True;"
  },
  "Jwt": {
    "Secret": "ScheduleApp_JWT_Dev_Secret_Key_MustBe32CharsOrMore!"
  }
}
```

- [ ] **Step 4: ビルドして通ることを確認する**

```bash
cd /Users/yoshizawayuki/ScheduleApp/backend/ScheduleApp.Api
dotnet build
```

期待結果: `Build succeeded`

---

## Task 15: テストユーザーのシードデータ作成

- [ ] **Step 1: BCrypt ハッシュを生成する**

`backend/ScheduleApp.Tests/` に一時ファイルを作成して実行し、ハッシュ値を取得する:

```bash
cat > /tmp/GenHash.cs << 'EOF'
using BCrypt.Net;
Console.WriteLine(BCrypt.HashPassword("test1234"));
EOF
cd /Users/yoshizawayuki/ScheduleApp/backend/ScheduleApp.Tests
dotnet script /tmp/GenHash.cs 2>/dev/null || dotnet-script /tmp/GenHash.cs 2>/dev/null || \
  dotnet run --project . -- 2>/dev/null
```

dotnet-script が使えない場合は、以下の xUnit テストを一時的に追加して出力を確認する:

```csharp
// backend/ScheduleApp.Tests/Services/AuthServiceTests.cs に一時追加
[Fact]
public void PrintHash()
{
    Console.WriteLine(BCrypt.Net.BCrypt.HashPassword("test1234"));
    Assert.True(true);
}
```

```bash
cd /Users/yoshizawayuki/ScheduleApp/backend
dotnet test --filter "PrintHash" -v normal 2>&1 | grep "\$2"
```

出力されたハッシュ文字列（`$2a$11$...` の形式）をメモしておく。

- [ ] **Step 2: メモしたハッシュを使って SQL Server にデータを挿入する**

`{HASH}` を Step 1 で取得したハッシュに置き換えて実行する:

```bash
docker exec scheduleapp-sqlserver \
  /opt/mssql-tools18/bin/sqlcmd \
  -S localhost -U sa -P 'ScheduleApp_2024!' -C \
  -Q "
IF NOT EXISTS (SELECT 1 FROM colors WHERE hex_code = '#FF8C00')
  INSERT INTO colors (hex_code, display_name, sort_order) VALUES ('#FF8C00', 'オレンジ', 1);

IF NOT EXISTS (SELECT 1 FROM groups WHERE name = 'テストグループ')
  INSERT INTO groups (name) VALUES ('テストグループ');

DECLARE @colorId INT = (SELECT id FROM colors WHERE hex_code = '#FF8C00');
DECLARE @groupId INT = (SELECT id FROM groups WHERE name = 'テストグループ');
IF NOT EXISTS (SELECT 1 FROM users WHERE login_id = 'admin')
  INSERT INTO users (login_id, name, password_hash, personal_color_id, theme_color_id, role, group_id, is_active)
  VALUES ('admin', '管理者', '{HASH}', @colorId, @colorId, 0, @groupId, 1);
"
```

- [ ] **Step 3: 挿入を確認する**

```bash
docker exec scheduleapp-sqlserver \
  /opt/mssql-tools18/bin/sqlcmd \
  -S localhost -U sa -P 'ScheduleApp_2024!' -C \
  -Q "SELECT id, login_id, name, group_id FROM users"
```

期待結果: `admin` ユーザーが1件表示される

---

## Task 16: フロントエンド – Vite プロキシ設定

**Files:**
- Create: `frontend/vite.config.ts`

- [ ] **Step 1: vite.config.ts を作成する**

```typescript
import { defineConfig } from 'vite';

export default defineConfig({
  server: {
    proxy: {
      '/api': {
        target:       'http://localhost:5296',
        changeOrigin: true,
      },
    },
  },
});
```

---

## Task 17: フロントエンド – CSS

**Files:**
- Create: `frontend/src/styles/main.css`
- Create: `frontend/src/pages/login/login.css`
- Modify: `frontend/index.html`

- [ ] **Step 1: main.css を作成する**

```css
:root {
  --theme-color:   #FF8C00;
  --bg-primary:    #3a3a3a;
  --bg-card:       #4a4a4a;
  --text-primary:  #ffffff;
  --input-bg:      #d0d0d0;
  --input-text:    #333333;
  --error-color:   #ff6b6b;
  --font-base:     16px;
}

*, *::before, *::after {
  box-sizing: border-box;
  margin:     0;
  padding:    0;
}

body {
  font-family:      sans-serif;
  font-size:        var(--font-base);
  background-color: var(--bg-primary);
  color:            var(--text-primary);
  min-height:       100vh;
}
```

- [ ] **Step 2: login.css を作成する**

`frontend/src/pages/login/login.css`:

```css
.login-page {
  display:         flex;
  justify-content: center;
  align-items:     center;
  min-height:      100vh;
}

.login-card {
  background-color: var(--bg-card);
  padding:          2.5rem 2rem;
  width:            320px;
  display:          flex;
  flex-direction:   column;
  gap:              1.5rem;
}

.login-field {
  display:        flex;
  flex-direction: column;
  gap:            0.5rem;
}

.login-field label {
  font-size: 0.9rem;
}

.login-field input {
  background-color: var(--input-bg);
  color:            var(--input-text);
  border:           none;
  padding:          0.6rem 0.5rem;
  font-size:        1rem;
  width:            100%;
}

.login-btn {
  background-color: var(--theme-color);
  color:            #ffffff;
  border:           none;
  padding:          0.8rem;
  font-size:        1rem;
  font-weight:      bold;
  cursor:           pointer;
  width:            100%;
}

.login-btn:disabled {
  opacity: 0.7;
  cursor:  not-allowed;
}

.login-error {
  color:      var(--error-color);
  font-size:  0.85rem;
  text-align: center;
  min-height: 1.2rem;
}
```

- [ ] **Step 3: index.html に main.css を追加する**

`<head>` 内の既存の `<link>` の下に追加する:

```html
<link rel="stylesheet" href="/src/styles/main.css" />
```

---

## Task 18: フロントエンド – auth.ts

**Files:**
- Create: `frontend/src/api/auth.ts`

- [ ] **Step 1: auth.ts を作成する**

```typescript
import { api } from './client';

export interface LoginRequest {
  loginId:  string;
  password: string;
}

export interface LoginResponse {
  userId:        number;
  name:          string;
  role:          number;
  themeColorHex: string;
}

export const auth = {
  login:  (data: LoginRequest) => api.post<LoginResponse>('/auth/login', data),
  logout: ()                   => api.post<null>('/auth/logout', {}),
};
```

---

## Task 19: フロントエンド – ログインページ

**Files:**
- Create: `frontend/src/pages/login/index.ts`

- [ ] **Step 1: login/index.ts を作成する**

```typescript
import { auth }     from '../../api/auth';
import { navigate } from '../../utils/router';
import             './login.css';

const ERROR_MESSAGES: Record<string, string> = {
  AUTH_INVALID_CREDENTIALS: 'IDまたはパスワードが違います',
  AUTH_ACCOUNT_DISABLED:    'アカウントが無効です',
  AUTH_NO_GROUP:            'グループに加入してください',
};

export function mount(app: HTMLElement): void {
  app.innerHTML = `
    <div class="login-page">
      <div class="login-card">
        <div class="login-field">
          <label for="loginId">ID</label>
          <input type="text" id="loginId" autocomplete="username" />
        </div>
        <div class="login-field">
          <label for="password">パスワード</label>
          <input type="password" id="password" autocomplete="current-password" />
        </div>
        <button id="loginBtn" class="login-btn">ログイン</button>
        <p id="loginError" class="login-error" aria-live="polite"></p>
      </div>
    </div>
  `;

  const btn     = app.querySelector<HTMLButtonElement>('#loginBtn')!;
  const errorEl = app.querySelector<HTMLParagraphElement>('#loginError')!;

  btn.addEventListener('click', async () => {
    const loginId  = app.querySelector<HTMLInputElement>('#loginId')!.value.trim();
    const password = app.querySelector<HTMLInputElement>('#password')!.value;

    errorEl.textContent = '';
    btn.disabled        = true;

    const result = await auth.login({ loginId, password });

    if (result.success && result.data) {
      document.documentElement.style.setProperty('--theme-color', result.data.themeColorHex);
      navigate('/home');
      return;
    }

    errorEl.textContent = ERROR_MESSAGES[result.error?.code ?? ''] ?? 'エラーが発生しました';
    btn.disabled        = false;
  });
}
```

---

## Task 20: ルーターの更新

**Files:**
- Modify: `frontend/src/utils/router.ts`

- [ ] **Step 1: '/' ルートを追加する**

`router.ts` の `routes` を以下に変更する:

```typescript
const routes: Record<string, PageLoader> = {
  '/': () => import('../pages/login'),
};
```

---

## Task 21: 動作確認

- [ ] **Step 1: バックエンドを起動する**

```bash
cd /Users/yoshizawayuki/ScheduleApp/backend/ScheduleApp.Api
dotnet run
```

期待結果: `Now listening on: http://localhost:5296`

- [ ] **Step 2: フロントエンドを起動する（別ターミナル）**

```bash
cd /Users/yoshizawayuki/ScheduleApp/frontend
npm run dev
```

期待結果: `Local: http://localhost:5173/`

- [ ] **Step 3: ブラウザで http://localhost:5173 を開く**

期待結果: ログイン画面が表示される（ID・パスワード欄・ログインボタン）

- [ ] **Step 4: 誤ったパスワードでログインする**

ID: `admin`、パスワード: `wrong` を入力してログインボタンを押す

期待結果: `IDまたはパスワードが違います` が表示される

- [ ] **Step 5: 正しい認証情報でログインする**

ID: `admin`、パスワード: `test1234` を入力してログインボタンを押す

期待結果: `/home` へ遷移する（`画面が見つかりません: /home` が表示されれば認証は成功）

- [ ] **Step 6: sessions テーブルにレコードが作成されたことを確認する**

```bash
docker exec scheduleapp-sqlserver \
  /opt/mssql-tools18/bin/sqlcmd \
  -S localhost -U sa -P 'ScheduleApp_2024!' -C \
  -Q "SELECT id, user_id, expires_at FROM sessions"
```

期待結果: 1件のレコードが存在する

---

## Task 22: 最終コミット

- [ ] **Step 1: 変更をすべてステージングしてコミットする**

```bash
cd /Users/yoshizawayuki/ScheduleApp
git add backend/ frontend/
git commit -m "feat: implement login screen (SCR-00) with JWT + DB session auth"
```
